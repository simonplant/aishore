# aishore configuration

project:
  name: "aishore"

validation:
  command: ""
  timeout: 120

# Backlog files to read from (relative to backlog/ directory).
# Omit to use the default: [backlog.json, bugs.json]
# backlog_files:
#   - backlog.json
#   - bugs.json
#   - infra.json

# models:
#   primary: "claude-opus-4-6"
#   fast: "claude-sonnet-4-6"

# agent:
#   timeout: 3600

# Maturity protocol: developer runs implement → critique → harden phases.
# Disable with --quick flag or set to false here.
# maturity:
#   enabled: true

# notifications:
#   on_complete: "notify-send 'aishore' \"Sprint $1: $2\""
#   # Examples:
#   #   Linux:  notify-send 'aishore' "Sprint $1: $2"
#   #   macOS:  osascript -e "display notification \"Sprint $1: $2\" with title \"aishore\""
#   #   Webhook: curl -s -X POST https://example.com/hook -d "{\"status\":\"$1\",\"item\":\"$2\"}"

# cost_tracking:
#   enabled: true          # Set to false to disable all token/cost tracking
#   pricing:
#     opus_in: 15.00       # USD per 1M input tokens for claude-opus-4-*
#     opus_out: 75.00      # USD per 1M output tokens for claude-opus-4-*
#     sonnet_in: 3.00      # USD per 1M input tokens for claude-sonnet-4-*
#     sonnet_out: 15.00    # USD per 1M output tokens for claude-sonnet-4-*
# Use --budget N with auto/run to stop when estimated cost approaches $N.
