{
  "description": "Bugs and tech debt",
  "items": [
    {
      "id": "BUG-001",
      "title": "AC verify report uses literal \\n instead of newlines",
      "intent": "AC verify report must render with real newlines so validator receives properly formatted output",
      "description": "run_ac_verification() builds the report string using '\\n' in bash double-quoted strings (e.g. report=\"${report}  PASS: $ac_text\\n\"). In bash, '\\n' inside double quotes is a literal backslash-n, not a newline. The garbled report is injected into validator prompts and retry context, degrading AI quality.",
      "priority": "should",
      "category": null,
      "steps": [
        "In .aishore/aishore, find run_ac_verification() (around line 1218)",
        "Locate all lines building the report variable with string concatenation using \\\\n",
        "Replace \\\\\"\\\\n\\\\\" inside double-quoted strings with $\\x27\\\\n\\x27 (ANSI-C quoting) or use printf to append newlines",
        "Verify the fix does not affect other uses of the report variable",
        "Run a sprint with AC verify commands and confirm report output contains real newlines"
      ],
      "acceptanceCriteria": [
        "AC verify report passed to validator contains actual newlines (not literal \\\\n)",
        "Retry prompt with AC failure context displays properly formatted multi-line output"
      ],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "groomedAt": "2026-03-20",
      "groomingNotes": "Confirmed: bash '\\n' in double-quoted strings does not produce newlines. Fix is straightforward — change to $'\\n' or printf. Low risk, small scope.",
      "completedAt": "2026-03-20T11:07:18-07:00"
    },
    {
      "id": "BUG-002",
      "title": "Merge conflict breaks multi-sprint loop without logging skipped items",
      "intent": "When a merge conflict aborts a multi-sprint run, the output must clearly list which items were not attempted",
      "description": "In cmd_run(), when _handle_sprint_success() returns non-zero (merge conflict), the loop breaks immediately. Items queued but not yet attempted are silently abandoned. The batch summary only covers items that were started. This makes it hard to know what needs to be re-run after resolving the conflict.",
      "priority": "should",
      "category": null,
      "steps": [
        "In .aishore/aishore, find the main sprint loop in cmd_run() that calls _handle_sprint_success()",
        "Before the loop, build an ordered list of all items that will be attempted in this batch",
        "On break (merge conflict), compute the set difference: all queued items minus started items",
        "Log each not-attempted item ID with a clear message before breaking",
        "Ensure print_batch_summary or a post-loop block surfaces the skipped items count and IDs",
        "Test with two queued items where the first causes a merge conflict"
      ],
      "acceptanceCriteria": [
        "After a merge conflict aborts a multi-sprint run, output lists item IDs that were queued but not started",
        "Batch summary accurately reflects items not attempted due to early abort"
      ],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "groomedAt": "2026-03-20",
      "groomingNotes": "Real UX gap: users lose track of which items were skipped after a merge conflict abort. Needs pre-loop item list and post-abort diff reporting.",
      "completedAt": "2026-03-20T11:14:54-07:00"
    },
    {
      "id": "BUG-003",
      "title": "Dead if/else branch in AC JSON construction is misleading",
      "intent": "AC JSON construction code must not have dead branches that confuse maintainers",
      "description": "In cmd_backlog_edit(), the AC entry builder has an if/else that checks whether an entry starts with '{' (JSON object) vs plain string, but both branches execute the identical statement: ac_json=\"$ac_json$entry\". The distinction is never acted upon. This is dead/misleading code that could hide future bugs if someone adds logic to only one branch.",
      "priority": "could",
      "category": null,
      "steps": [
        "In .aishore/aishore, find the AC JSON construction block in cmd_backlog_edit() (around line 3688)",
        "Identify the if/else that checks whether an entry starts with \"{\" — both branches do the same thing",
        "Remove the if/else and keep just the single statement: ac_json=\"$ac_json$entry\"",
        "Verify that backlog edit --ac and --ac-verify still produce valid JSON items",
        "Run .aishore/aishore backlog check on an item with AC entries to confirm no regression"
      ],
      "acceptanceCriteria": [
        "The if/else branch is removed, only the single common statement remains",
        "backlog edit --ac and --ac-verify still produce valid JSON items",
        "backlog check on an item with AC entries shows no regression"
      ],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "groomedAt": "2026-03-20",
      "groomingNotes": "Tech debt: dead if/else branches mislead future developers. Trivial fix — remove the conditional.",
      "completedAt": "2026-03-20T12:14:19-07:00"
    },
    {
      "id": "BUG-004",
      "title": "AC verify command output truncated at 1000 bytes, may cut mid-character",
      "intent": "AC verification failure output must be complete enough to diagnose the problem",
      "description": "run_ac_verification() uses 'head -c 1000' to cap output from AC verify commands. This can cut multi-byte UTF-8 characters in half, producing garbled text. Even for ASCII, 1000 bytes is often too small for test output. The truncated output is injected into prompts, where incomplete stack traces or error messages reduce the AI's ability to fix issues.",
      "priority": "should",
      "category": null,
      "steps": [
        "In .aishore/aishore, find run_ac_verification() where it uses head -c 1000 to cap output",
        "Replace byte-based truncation (head -c 1000) with line-based truncation (e.g. tail -n 50 or head -n 50)",
        "Or use a higher byte limit (e.g. 5000) that is unlikely to split useful error output",
        "Consider adding a truncation indicator (e.g. \"[... output truncated]\") so the AI knows output was cut",
        "Verify AC verify failure output is readable in retry prompts after the change"
      ],
      "acceptanceCriteria": [
        "AC verify output is not split mid-character (use line-based truncation instead of byte truncation)",
        "Truncation limit is configurable or set to a value adequate for typical test output (e.g., 3000 chars)"
      ],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "groomedAt": "2026-03-20",
      "groomingNotes": "PO review: bumped to should. Truncated error output directly hurts sprint recovery — the AI has less signal to fix failures. More failures = less value delivered. 1000 byte limit is too small for real test output and the byte-split-char bug is a correctness issue.",
      "completedAt": "2026-03-20T11:17:40-07:00"
    },
    {
      "id": "BUG-005",
      "title": "config.yaml silently ignored when yq unavailable and no warning shown",
      "intent": "Users must be clearly warned when their config.yaml changes are being silently ignored due to missing yq",
      "description": "load_config() falls back to grep-based parsing when yq is not installed. This fallback is much less reliable and can silently ignore valid configuration. The warning is only triggered if uncommented settings are detected by grep. A config file that uses valid YAML but is structured in a way grep misses will be silently ignored, leaving the user thinking their overrides are active when they are not.",
      "priority": "must",
      "category": null,
      "steps": [
        "In .aishore/aishore, find load_config() where yq availability is checked (around line 267)",
        "Add an early check: if config.yaml exists and has non-empty content, and yq is not available, always emit a warning",
        "Warning should name the config file, explain what is being ignored, and give install instructions for yq",
        "Ensure the warning is shown regardless of whether grep finds any settings",
        "Test by creating a config.yaml with a setting and running aishore without yq installed"
      ],
      "acceptanceCriteria": [
        "When yq is absent and config.yaml exists with content, a clear warning is always shown regardless of grep detection",
        "Warning text explains what yq is and how to install it"
      ],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "groomedAt": "2026-03-20",
      "groomingNotes": "PO re-review (failCount: 2): Two sprint attempts rejected. The validator has confirmed: the warning must fire unconditionally when config.yaml exists with non-empty content and yq is absent. The grep-detection fallback MUST NOT gate the warning — this was the failure mode in both attempts. Implementation checklist: (1) check if yq is absent, (2) check if config.yaml exists and is non-empty, (3) if both true, print warning immediately before any grep fallback logic runs. Do not mix the warning into the grep-detection branch. The warning is unconditional; grep-based fallback is optional and may still run after warning. Must fix before new-user onboarding can be considered reliable.",
      "lastFailReason": "Validator agent rejected implementation",
      "lastFailAt": "2026-03-20T15:33:03-07:00",
      "failCount": 4,
      "completedAt": "2026-03-20T16:04:47-07:00"
    },
    {
      "id": "BUG-006",
      "title": "backlog add and edit have no --step flag for setting implementation steps",
      "intent": "Users must be able to add and replace implementation steps via CLI without hand-editing JSON — steps are required for sprint readiness and a missing flag forces a manual workaround.",
      "description": "backlog add creates items with steps: [] and backlog edit has no flag to append or replace steps. The only way to populate the steps array is to edit the backlog JSON directly, which violates the CLI-first design. Both commands already follow a consistent flag pattern (--ac for AC entries, --scope for scope patterns). Steps should follow the same pattern: --step adds one step (repeatable), --clear-steps resets the array. This unblocks grooming items without requiring JSON editing.",
      "priority": "should",
      "category": null,
      "steps": [],
      "acceptanceCriteria": [
        "backlog add --step 'Do X' creates an item with steps: [\"Do X\"]",
        "backlog add --step 'Do X' --step 'Do Y' creates steps in order",
        "backlog edit <ID> --step 'Do X' appends X to the existing steps array",
        "backlog edit <ID> --clear-steps resets steps to empty array"
      ],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "groomedAt": "2026-03-20",
      "groomingNotes": "New item added during groom session. Implementation pattern mirrors --ac/--scope: local steps array, --step appends one entry per flag call, --clear-steps resets. In cmd_backlog_add(): add local steps_entries=() array, --step flag appends to it, build steps_json from the array, pass --argjson steps_arr to add_item, set steps: $steps_arr (replacing the hardcoded steps: []). In cmd_backlog_edit(): add --step flag that accumulates entries same as --ac, build steps JSON, use jq_updates += '| .steps = ((.steps // []) + $newsteps)'. Add --clear-steps: jq_updates += '| .steps = []'. --ready flag warning about no steps is expected (bootstrapping issue).",
      "completedAt": "2026-03-20T11:58:36-07:00"
    },
    {
      "id": "BUG-007",
      "title": "backlog show renders AC {text, verify} objects as raw JSON instead of plain text",
      "intent": "backlog show must display acceptance criteria as readable text — when entries are {text, verify} objects, only the text should be shown, not the JSON structure.",
      "description": "When backlog edit --ac 'Some check' --ac-verify 'make test' is used, the AC entry is stored as a JSON object {\"text\": \"Some check\", \"verify\": \"make test\"}. In cmd_backlog_show(), the jq display expression at line ~3720 uses (.acceptanceCriteria // [] | .[] | \"  - \\(.)\"), which calls tojson on the entire object, producing output like: '  - {\"text\":\"Some check\",\"verify\":\"make test\"}'. The fix is to check entry type: if it's a string, show it as-is; if it's an object, extract .text (and optionally show the verify command in a shorter form).",
      "priority": "should",
      "category": null,
      "steps": [],
      "acceptanceCriteria": [
        "backlog show <ID> displays plain-string AC entries unchanged",
        "backlog show <ID> displays {text, verify} AC entries as the .text value only (not raw JSON)",
        "backlog show <ID> optionally shows verify command on same or next line in a readable format"
      ],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "groomedAt": "2026-03-20",
      "groomingNotes": "Fix location: cmd_backlog_show() jq expression around line 3720. Current: (.acceptanceCriteria // [] | .[] | \"  - \\(.)\"). Fix: (.acceptanceCriteria // [] | .[] | \"  - \\(if type == \"object\" then .text + (if .verify then \" [verify: \\(.verify)]\" else \"\" end) else . end)\"). Run: backlog add with --ac 'plain' --ac 'text' --ac-verify 'cmd' then backlog show to confirm. This is a display-only fix — no data changes.",
      "completedAt": "2026-03-20T12:00:45-07:00"
    },
    {
      "id": "BUG-008",
      "title": "VALIDATE_CMD output injected untruncated into retry prompts",
      "intent": "Retry prompts must not contain unbounded validation output — token bloat degrades AI fix quality and can push critical context out of attention.",
      "description": "In _run_retry_loop(), validate_output is captured with 'cat $validate_tmpfile' (no truncation limit) and injected raw into retry_extra_prompt. run_ac_verification() and the pre-flight check both apply OUTPUT_TRUNCATE_LINES truncation, but the VALIDATE_CMD retry path does not. For verbose test suites (cargo test, pytest, mocha), this produces thousands of lines in the retry prompt, degrading AI quality. The validator-agent retry path at line ~1769 has the same issue.",
      "priority": "should",
      "category": null,
      "steps": [
        "In .aishore/aishore, find _run_retry_loop() and locate the validate_output capture: validate_output=$(cat \"$validate_tmpfile\" 2>/dev/null || true)",
        "Replace the cat with the same truncation pattern used in run_ac_verification(): capture wc -l, use tail -n $OUTPUT_TRUNCATE_LINES, prepend '[... output truncated, showing last N of M lines]' if truncated",
        "Verify the validator-agent retry path also uses the truncated validate_output (line ~1769)",
        "Confirm pre-flight check already has truncation (it does, around line 1650); no change needed there"
      ],
      "acceptanceCriteria": [
        "VALIDATE_CMD output injected into retry prompts is truncated to OUTPUT_TRUNCATE_LINES lines",
        "When output is truncated, the retry prompt includes a '[... output truncated, showing last N of M lines]' indicator",
        "Truncation matches the pattern used in run_ac_verification() for consistency"
      ],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "groomedAt": "2026-03-20",
      "groomingNotes": "Same pattern as BUG-004 (AC verify truncation) but missed for the VALIDATE_CMD retry path. Fix is additive — apply OUTPUT_TRUNCATE_LINES truncation at one spot in _run_retry_loop(). Low risk, clear scope.",
      "completedAt": "2026-03-20T12:08:42-07:00"
    },
    {
      "id": "BUG-009",
      "title": "backlog edit has no --clear-ac flag to reset acceptance criteria",
      "intent": "Users must be able to reset AC via CLI — without --clear-ac, wrong AC entries require hand-editing JSON, violating the CLI-first design principle.",
      "description": "backlog edit --step has a companion --clear-steps flag; backlog edit --scope has a companion --clear-scope flag. But --ac has no --clear-ac. Once AC entries are added, the only way to remove them is to hand-edit the backlog JSON. This is inconsistent with the established pattern and violates CLI-first design. Fix: add --clear-ac to cmd_backlog_edit() that resets acceptanceCriteria to [], following the same pattern as --clear-steps.",
      "priority": "should",
      "category": null,
      "steps": [
        "In .aishore/aishore, find cmd_backlog_edit() and locate the --clear-steps case as reference (around line 3843-3846)",
        "Add a --clear-ac case: set jq_updates with .acceptanceCriteria = [] and update update_desc",
        "Add --clear-ac documentation to the help text in cmd_backlog_edit error message and cmd_help",
        "Test: backlog edit BUG-003 --clear-ac (verify AC resets), then backlog edit BUG-003 --ac 'New' (verify fresh entry appears)"
      ],
      "acceptanceCriteria": [
        "backlog edit <ID> --clear-ac resets acceptanceCriteria to []",
        "backlog edit <ID> --clear-ac --ac 'New AC' results in only the new entry present",
        "--clear-ac appears in the backlog edit help output"
      ],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "groomedAt": "2026-03-20",
      "groomingNotes": "Clear pattern mismatch — --clear-steps and --clear-scope both exist, --clear-ac does not. Fix is additive, one case block in cmd_backlog_edit plus help text update. Low risk.",
      "completedAt": "2026-03-20T12:11:16-07:00"
    },
    {
      "id": "BUG-010",
      "title": "remove_items_by_status takes full path while other CRUD helpers take relative filename",
      "intent": "Backlog CRUD helpers must use a consistent path convention — the current asymmetry is a latent footgun that will cause silent wrong-file operations when new callers are added.",
      "description": "update_item() and remove_item() accept a relative filename (e.g. 'backlog.json') and prepend BACKLOG_DIR internally. remove_items_by_status() takes a full path directly. Callers today are consistent, but the asymmetry is not documented and will cause bugs when someone adds a new call site with the wrong convention. Fix: change remove_items_by_status to accept a relative filename and prepend BACKLOG_DIR internally, matching the established pattern.",
      "priority": "could",
      "category": null,
      "steps": [
        "In .aishore/aishore, find remove_items_by_status() around line 703 and update it to accept a relative filename like update_item() and remove_item()",
        "Change the jq command argument from '$file' to '$BACKLOG_DIR/$file'",
        "Update cmd_clean() callers (line ~4025-4026) to pass just the filename ('backlog.json', 'bugs.json') instead of the full path variables",
        "Run .aishore/aishore clean --dry-run and then clean on a test setup to confirm no regression"
      ],
      "acceptanceCriteria": [
        "remove_items_by_status accepts a relative filename consistent with update_item and remove_item",
        "cmd_clean still correctly removes done items after the refactor",
        "grep confirms no other callers of remove_items_by_status exist with full paths"
      ],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "groomedAt": "2026-03-20",
      "groomingNotes": "Tech debt: path convention inconsistency between remove_items_by_status and the other CRUD helpers. Small refactor with one call site to update. Low risk — only one caller exists today (cmd_clean).",
      "completedAt": "2026-03-20T12:27:27-07:00"
    },
    {
      "id": "BUG-011",
      "title": "cmd_review silently ignores unknown options",
      "intent": "cmd_review must reject unknown flags immediately — silently discarding options lets typos pass undetected and erodes user trust in the CLI",
      "description": "cmd_review() parses --update-docs and --since but its catch-all branch is '* ) shift ;;' at line 2414, silently discarding any unrecognized flag. Most other commands (cmd_clean, cmd_backlog_add, etc.) reject unknown options with log_error and return 1. A user who mistypes '--update_docs' or '--since-commit' will get no error — the command runs silently in the wrong mode. Fix: replace the silent '* ) shift' with '* ) log_error \"Unknown option: $1\"; return 1' to match the established pattern.",
      "priority": "should",
      "category": null,
      "steps": [
        "In .aishore/aishore, find cmd_review() and locate the option parsing loop (around line 2410-2416)",
        "Replace the catch-all '* ) shift ;;' with '* ) log_error \"Unknown option: $1\"; return 1 ;;'",
        "Verify that valid options --update-docs and --since still work normally",
        "Verify that 'aishore review --typo' now prints an error and exits non-zero"
      ],
      "acceptanceCriteria": [
        "aishore review --unknown-flag exits with a non-zero status and prints an error message",
        "aishore review --update-docs and aishore review --since <commit> still work correctly",
        "Error message matches the format used by other commands: 'Unknown option: <flag>'"
      ],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "groomedAt": "2026-03-20",
      "groomingNotes": "Confirmed at line 2414: catch-all '* ) shift' drops unknown flags silently. Pattern inconsistency vs cmd_clean, cmd_backlog_add and others which all use log_error + return 1. One-line fix. Low risk — additive error handling only.",
      "completedAt": "2026-03-20T12:25:52-07:00"
    },
    {
      "id": "BUG-012",
      "title": "cmd_update silently ignores unknown options",
      "intent": "cmd_update must reject unknown flags — silently discarding options can make users believe a flag like --force or --dry-run took effect when it did not, potentially causing unintended updates",
      "description": "cmd_update() parses --dry-run, --check, --force, and --no-verify but its catch-all is '* ) shift ;;' at line 3174, silently discarding unknown flags. This is especially dangerous here: a user who mistypes '--dry-run' (e.g., '--dryrun' or '--dry_run') will get no error and the update will proceed as a live update — exactly the behavior they were trying to avoid. Fix: replace '* ) shift' with '* ) log_error \"Unknown option: $1\"; return 1' to match the established pattern in cmd_clean, cmd_backlog_add, etc.",
      "priority": "must",
      "category": null,
      "steps": [
        "In .aishore/aishore, find cmd_update() and locate the option parsing loop (around line 3169-3176)",
        "Replace the catch-all '* ) shift ;;' with '* ) log_error \"Unknown option: $1\"; return 1 ;;'",
        "Verify that valid options --dry-run, --force, --no-verify still work normally",
        "Verify that 'aishore update --typo' now prints an error and exits non-zero"
      ],
      "acceptanceCriteria": [
        "aishore update --unknown-flag exits with a non-zero status and prints an error message",
        "aishore update --dry-run, --force, and --no-verify --force all still work correctly",
        "Error message matches the format used by other commands: 'Unknown option: <flag>'"
      ],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "groomedAt": "2026-03-20",
      "groomingNotes": "Bumped to must: a mistyped --dry-run (e.g. --dryrun or --dry_run) silently executes a live update — the user explicitly tried to preview and got destructive action with no warning. This is the same class of failure as BUG-005 (silent config ignore). update is a state-changing, potentially irreversible operation; the --dry-run flag is a safety net that must be reliable. One-line fix, but high consequence if missed.",
      "completedAt": "2026-03-20T12:23:47-07:00"
    },
    {
      "id": "BUG-013",
      "title": "cmd_groom and cmd_metrics silently accept unknown options",
      "intent": "All CLI commands must reject unknown flags — silent acceptance of garbage input makes the CLI unpredictable and violates the established error-handling pattern",
      "description": "cmd_groom() at line 2262 checks only '${1:-}' == '--backlog' without a proper arg-parsing loop, so any extra argument after --backlog is silently ignored. cmd_metrics() at line 2470 does the same: checks only '${1:-}' == '--json'. Neither reports an error for unknown flags like '--bogus'. Both should use a proper while/case loop with a catch-all that calls log_error and returns 1, consistent with cmd_clean, cmd_backlog_add, and other commands in the same script.",
      "priority": "could",
      "category": null,
      "steps": [
        "In .aishore/aishore, find cmd_groom() (line 2258) and replace the single-line check '[\\[ \"${1:-}\" == \"--backlog\" ]]' with a proper while/case loop that handles --backlog and rejects unknown options",
        "In .aishore/aishore, find cmd_metrics() (line 2466) and replace the single-line check '[\\[ \"${1:-}\" == \"--json\" ]]' with a proper while/case loop that handles --json and rejects unknown options",
        "Verify that 'aishore groom --backlog' and 'aishore groom' still work correctly",
        "Verify that 'aishore metrics --json' and 'aishore metrics' still work correctly",
        "Verify that 'aishore groom --typo' and 'aishore metrics --typo' now print an error and exit non-zero"
      ],
      "acceptanceCriteria": [
        "aishore groom --unknown-flag exits with a non-zero status and prints 'Unknown option: --unknown-flag'",
        "aishore metrics --unknown-flag exits with a non-zero status and prints 'Unknown option: --unknown-flag'",
        "aishore groom, aishore groom --backlog, aishore metrics, and aishore metrics --json all continue to work correctly"
      ],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "groomedAt": "2026-03-20",
      "groomingNotes": "Both cmd_groom (line 2262) and cmd_metrics (line 2470) use single-check pattern instead of proper while/case loop. Lower severity than BUG-011/012 since these commands have fewer dangerous flags, but still violates CLI consistency. Two functions to fix, same pattern each. Low risk.",
      "completedAt": "2026-03-20T12:43:00-07:00"
    },
    {
      "id": "BUG-014",
      "title": "cmd_backlog_populate silently ignores unknown options",
      "intent": "cmd_backlog_populate must reject unknown flags — silently discarding options is inconsistent with the established error-handling pattern and hides user mistakes",
      "description": "cmd_backlog_populate() (around line 3457) performs no option parsing at all. Any flag passed to 'backlog populate' is silently ignored. Most other commands use a while/case loop with a catch-all that calls log_error and returns 1. Fix: add a proper option parsing loop. populate currently takes no flags, so any argument is an error — the loop body should be just the catch-all that rejects everything.",
      "priority": "should",
      "category": null,
      "steps": [
        "In .aishore/aishore, find cmd_backlog_populate() and identify the current argument handling (none)",
        "Add a while/case loop before the function body that rejects unknown options with log_error 'Unknown option: $1' and returns 1",
        "Verify that 'backlog populate' with no arguments still works correctly",
        "Verify that 'backlog populate --typo' now prints an error and exits non-zero"
      ],
      "acceptanceCriteria": [
        "backlog populate --unknown-flag exits with a non-zero status and prints 'Unknown option: --unknown-flag'",
        "backlog populate with no arguments still works correctly",
        "Error message matches the format used by other commands"
      ],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "groomedAt": "2026-03-20",
      "groomingNotes": "No option parsing at all in cmd_backlog_populate. Fix is additive — add a while/case loop with a catch-all that rejects all arguments since populate takes no flags. Low risk, same pattern as BUG-011/012/013.",
      "completedAt": "2026-03-20T12:35:08-07:00"
    },
    {
      "id": "BUG-015",
      "title": "cmd_run treats unknown flags as item IDs instead of erroring",
      "intent": "cmd_run must reject unrecognized flags — silently accepting unknown options as item IDs causes confusing behavior where typos like --dry_run are interpreted as sprint IDs and silently do nothing",
      "description": "cmd_run() has a catch-all in its option parsing loop (around line 1905) that accepts any non-flag positional argument as either a count or a specific item ID. This means unknown flags like '--dry_run' (intended as --dry-run) or '--retries' (missing value) are silently interpreted as item IDs, producing a 'no item found' error rather than 'unknown option'. The fix is to detect that an argument starts with '--' in the catch-all and reject it as an unknown option before treating it as a positional.",
      "priority": "should",
      "category": null,
      "steps": [
        "In .aishore/aishore, find cmd_run() and locate the catch-all in the option parsing loop (around line 1905-1912)",
        "In the catch-all branch, before the existing logic, add: if [[ \"$1\" == --* ]]; then log_error \"Unknown option: $1\"; return 1; fi",
        "Verify that 'run 3', 'run FEAT-001', and 'run' without arguments still work correctly",
        "Verify that 'run --typo' now prints an error and exits non-zero"
      ],
      "acceptanceCriteria": [
        "aishore run --unknown-flag exits with a non-zero status and prints an error message",
        "aishore run N (numeric count), aishore run <ID>, and aishore run still work correctly",
        "Error message matches the format used by other commands: 'Unknown option: <flag>'"
      ],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "groomedAt": "2026-03-20",
      "groomingNotes": "Catch-all in cmd_run interprets --flag-typos as item IDs. Fix is a single guard in the catch-all: check if arg starts with '--' and reject it. Low risk — only changes error behavior, not normal paths.",
      "completedAt": "2026-03-20T12:37:09-07:00"
    },
    {
      "id": "BUG-016",
      "title": "cmd_backlog_edit usage message omits most supported flags",
      "intent": "The backlog edit help text must list all supported flags — a usage message that omits 9 of 18 flags is misleading and makes the CLI appear less capable than it is",
      "description": "cmd_backlog_edit() has two help text locations that list supported flags: the early usage error message (around line 3767) and the catch-all error at the end (around line 3941). The early message lists only: --title, --intent, --priority, --status, --desc, --category, --depends-on, --ready, --no-ready. It omits: --step, --clear-steps, --scope, --clear-scope, --ac, --ac-verify, --clear-ac, --groomed-at, --groomed-notes. The catch-all message at line 3941 is complete. Fix: update the early usage message to match the complete list in the catch-all.",
      "priority": "should",
      "category": null,
      "steps": [
        "In .aishore/aishore, find cmd_backlog_edit() and locate the early usage error message (around line 3767) that is shown when no ID is provided",
        "Compare it to the complete flag list in the catch-all error message (around line 3941)",
        "Update the early usage message to include all supported flags: --step, --clear-steps, --scope, --clear-scope, --ac, --ac-verify, --clear-ac, --groomed-at, --groomed-notes",
        "Verify that 'backlog edit' with no arguments shows the updated usage message"
      ],
      "acceptanceCriteria": [
        "Running 'backlog edit' with no arguments shows a usage message that includes --step, --clear-steps, --ac, --ac-verify, --clear-ac, --scope, --clear-scope, --groomed-at, --groomed-notes",
        "The complete flag list in the catch-all error message is unchanged",
        "No functional behavior changes — this is display only"
      ],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "groomedAt": "2026-03-20",
      "groomingNotes": "PO bump to should: flags like --step, --clear-steps, --ac, --ac-verify, --clear-ac were added specifically to enable CLI-first grooming without JSON editing. Hiding 9 of 18 flags in the early usage message undermines the discoverability of that entire investment. Users who type 'backlog edit' to learn the options will think those features don't exist. This is a documentation debt that defeats the purpose of BUG-006, BUG-007, and BUG-009.",
      "completedAt": "2026-03-20T12:39:01-07:00"
    },
    {
      "id": "BUG-017",
      "title": "cmd_backlog_add missing validate_arg calls for most flags",
      "intent": "backlog add must validate that flag arguments are non-empty — silently accepting empty values creates malformed backlog items that fail sprint readiness checks without a clear error message",
      "description": "cmd_backlog_add() calls validate_arg for --step (line 3617) but not for --type, --title, --intent, --desc, --priority, --category, or --depends-on (lines 3592-3600). This means 'backlog add --title' (with no value) silently sets title to an empty string, producing a malformed item. By contrast, cmd_backlog_edit() consistently calls validate_arg for all value-bearing flags. Fix: add validate_arg calls in cmd_backlog_add() to match the pattern already established in cmd_backlog_edit().",
      "priority": "should",
      "category": null,
      "steps": [
        "In .aishore/aishore, find cmd_backlog_add() and locate the flag handling block (around lines 3592-3617)",
        "For each flag that takes a value (--type, --title, --intent, --desc, --priority, --category, --depends-on), add validate_arg \"$1\" \"${2:-}\" || return 1 before the assignment, matching the pattern in cmd_backlog_edit()",
        "Verify that 'backlog add --title' (no value) now prints an error and exits non-zero",
        "Verify that 'backlog add --title \"My Item\" --priority must' still creates the item correctly"
      ],
      "acceptanceCriteria": [
        "backlog add --title (with no value) exits with a non-zero status and prints an error",
        "backlog add --intent (with no value) exits with a non-zero status and prints an error",
        "backlog add with all required values still creates items correctly",
        "validate_arg is called for --type, --title, --intent, --desc, --priority, --category, and --depends-on"
      ],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "groomedAt": "2026-03-20",
      "groomingNotes": "cmd_backlog_edit already validates all flags with validate_arg; cmd_backlog_add inconsistently skips it for most flags. Additive fix — add validate_arg calls. Low risk, small scope.",
      "completedAt": "2026-03-20T12:41:01-07:00"
    },
    {
      "id": "BUG-018",
      "title": "cmd_backlog_show and cmd_backlog_check silently accept extra positional arguments",
      "intent": "backlog show and backlog check must reject extra arguments — silently ignoring them makes 'backlog show FEAT-001 FEAT-002' appear to succeed while only processing the first ID, giving users false confidence",
      "description": "cmd_backlog_show() extracts $1 as the item ID and ignores anything after it. cmd_backlog_check() does the same. Both commands silently accept extra arguments: 'backlog show FEAT-001 FEAT-002' processes only FEAT-001 without any error, which is misleading. The fix for each is to check that exactly one positional argument was given and reject anything extra with log_error.",
      "priority": "could",
      "category": null,
      "steps": [
        "In .aishore/aishore, find cmd_backlog_show() and after extracting the ID from $1, check if $# -gt 1 and emit log_error 'Unexpected argument: ${2}' and return 1",
        "In .aishore/aishore, find cmd_backlog_check() and apply the same fix — after extracting the ID, check for extra arguments and error",
        "Verify that 'backlog show FEAT-001' and 'backlog check FEAT-001' still work correctly",
        "Verify that 'backlog show FEAT-001 FEAT-002' and 'backlog check FEAT-001 --typo' now print an error and exit non-zero"
      ],
      "acceptanceCriteria": [
        "backlog show <ID> <extra> exits non-zero and prints an error about the unexpected argument",
        "backlog check <ID> <extra> exits non-zero and prints an error about the unexpected argument",
        "backlog show <ID> and backlog check <ID> with exactly one argument still work correctly"
      ],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "groomedAt": "2026-03-20",
      "groomingNotes": "Both cmd_backlog_show and cmd_backlog_check silently drop extra positional args. Additive check: if $# > 1 after extracting ID, error out. Low risk, two functions to fix.",
      "completedAt": "2026-03-20T12:55:35-07:00"
    },
    {
      "id": "BUG-019",
      "title": "cmd_backlog_check returns 0 when readiness gates fail",
      "intent": "backlog check must return non-zero when gates fail — callers and scripts must be able to rely on exit status to detect unready items.",
      "description": "cmd_backlog_check() calls check_readiness_gates() and logs warnings on failure, but does not return 1. The function exits 0 regardless of gate outcome. Any script using 'backlog check $ID && do_something' will proceed even when gates fail, undermining the purpose of the check command. Fix: add 'return 1' in the else branch after printing gate warnings.",
      "priority": "should",
      "category": null,
      "steps": [
        "In .aishore/aishore, find cmd_backlog_check() (around line 3456)",
        "After the 'echo -e \"$CHECK_GATES_WARNINGS\"' line in the else branch, add 'return 1'",
        "Verify that 'backlog check <ready-ID>' exits 0 and 'backlog check <unready-ID>' exits non-zero"
      ],
      "acceptanceCriteria": [
        "backlog check <ID> exits non-zero when any readiness gate fails",
        "backlog check <ID> exits 0 when all readiness gates pass",
        "Exit status matches the log output — if warnings are printed, exit is non-zero"
      ],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "groomedAt": "2026-03-20",
      "groomingNotes": "Clear behavioral bug: gates failing but exit 0 is wrong. One-line fix — add return 1 in else branch. Low risk, high value for automation users.",
      "completedAt": "2026-03-20T12:54:00-07:00"
    },
    {
      "id": "BUG-020",
      "title": "cmd_backlog_edit flushes pending AC entry without JSON encoding",
      "intent": "AC entries appended via --ac must be correctly JSON-encoded — unencoded special characters silently produce invalid JSON and corrupt the backlog.",
      "description": "In cmd_backlog_edit(), when a second --ac flag is encountered, the pending AC entry from the prior --ac is flushed to ac_entries with 'ac_entries+=(\"$pending_ac\")' (line ~3898) — no JSON encoding. cmd_backlog_add() correctly uses 'ac_entries+=(\"\"\\\"$pending_ac\\\"\"\")' in the same flush path. If the AC text contains '\"', backslash, or other JSON metacharacters, the constructed JSON will be invalid, silently corrupting the item's acceptanceCriteria. The final-flush path at line ~3942 IS correctly encoded — only the mid-loop flush is broken.",
      "priority": "must",
      "category": null,
      "steps": [
        "In .aishore/aishore, find cmd_backlog_edit() and locate the --ac case (around line 3894)",
        "Find the flush block: 'if [[ -n \"$pending_ac\" ]]; then ac_entries+=(\"$pending_ac\"); fi'",
        "Replace the flush with the encoded form matching cmd_backlog_add: 'ac_entries+=(\"\\\"\\\"$pending_ac\\\"\"\")'",
        "Verify: 'backlog edit <ID> --ac 'has \"quotes\"' --ac 'second entry'' produces valid JSON in the backlog file"
      ],
      "acceptanceCriteria": [
        "backlog edit <ID> --ac 'text with \"quotes\"' --ac 'second' stores both entries with proper JSON encoding",
        "backlog edit with plain AC entries (no special chars) still works correctly after the fix"
      ],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "groomedAt": "2026-03-20",
      "groomingNotes": "PO bump to must: silent JSON corruption of backlog data when AC text contains quotes/backslash is in the same class as BUG-005 — user does the right thing, gets wrong behavior, no signal. A corrupted item fails at sprint time with a cryptic error, not at edit time where the fix is obvious. The mid-loop flush is the single broken path; final-flush is correct. One-line fix with high consequence if left unresolved.",
      "completedAt": "2026-03-20T12:52:08-07:00"
    },
    {
      "id": "BUG-021",
      "title": "backlog list --status and --type flags missing validate_arg for empty values",
      "intent": "backlog list must reject flag calls with missing values — silently accepting an empty filter silently changes which items are shown, leading to misleading output.",
      "description": "In cmd_backlog_list(), the --status and --type flag cases do 'filter_status=\"$2\"; shift 2' without calling validate_arg first. If a user runs 'backlog list --status' without a value, $2 is empty, filter_status gets set to empty string, and the subsequent jq filter produces unexpected results. cmd_backlog_add and cmd_backlog_edit consistently call validate_arg for all value-bearing flags. The fix is to add validate_arg calls in the --status and --type cases, consistent with the established pattern.",
      "priority": "could",
      "category": null,
      "steps": [
        "In .aishore/aishore, find cmd_backlog_list() and locate the --status and --type flag cases (around line 3546-3552)",
        "Add 'validate_arg \"$1\" \"${2:-}\" || return 1' before each assignment, matching the pattern in cmd_backlog_add/edit",
        "Verify that 'backlog list --status' (no value) prints an error and exits non-zero",
        "Verify that 'backlog list --type' (no value) prints an error and exits non-zero",
        "Verify that 'backlog list --status todo' and 'backlog list --type bug' still work correctly"
      ],
      "acceptanceCriteria": [
        "backlog list --status (no value) exits non-zero with an error message",
        "backlog list --type (no value) exits non-zero with an error message",
        "backlog list --status todo and backlog list --type bug still work correctly"
      ],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "groomedAt": "2026-03-20",
      "groomingNotes": "Same pattern as BUG-017 (cmd_backlog_add missing validate_arg) but for cmd_backlog_list. Two flag cases need validate_arg added. Low risk, additive only.",
      "completedAt": "2026-03-20T13:33:11-07:00"
    },
    {
      "id": "BUG-022",
      "title": "cmd_clean prints success even when remove_items_by_status fails",
      "intent": "cmd_clean must not report successful cleanup if the backlog file was not actually updated — silent data loss on I/O error is worse than a visible failure.",
      "description": "In cmd_clean(), remove_items_by_status() is called but its return code is not checked. If jq fails (malformed JSON, disk full) or the mv that replaces the file fails, the command continues and prints 'Cleaned N features/bugs' as though it succeeded. The user believes the backlog was cleaned when it was not. Fix: check return code of both remove_items_by_status calls and exit non-zero with a clear error if either fails.",
      "priority": "should",
      "category": null,
      "steps": [
        "In .aishore/aishore, find cmd_clean() and locate the two remove_items_by_status calls (around line 4067-4068)",
        "Capture the return code of each call: 'remove_items_by_status ... || { log_error \"Failed to clean backlog.json\"; return 1; }'",
        "Ensure the success message is only printed after both calls succeed",
        "Verify: run clean on a valid setup (success printed), then test with a read-only backlog file (error printed, no success message)"
      ],
      "acceptanceCriteria": [
        "cmd_clean exits non-zero and prints an error when remove_items_by_status fails",
        "cmd_clean success message is only printed when cleanup actually succeeds",
        "cmd_clean --dry-run is not affected (no file writes in dry-run path)"
      ],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "groomedAt": "2026-03-20",
      "groomingNotes": "Silent success on write failure is the same class as BUG-005 (user does right thing, gets wrong behavior, no signal). The return code is not checked after remove_items_by_status — add || return 1 guard on both calls. Low risk, additive error handling only.",
      "completedAt": "2026-03-20T13:12:54-07:00"
    },
    {
      "id": "BUG-023",
      "title": "mark_item_failed silently swallows update errors with || true",
      "intent": "Sprint failure context must be recorded reliably — if mark_item_failed silently does nothing, subsequent retries and auto mode have no failure history to work from.",
      "description": "In mark_item_failed(), the call to update_item() is followed by '2>/dev/null || true', which suppresses all errors and always succeeds. If the backlog file write fails (bad JSON, permissions, missing file), the item is never marked failed and no error is surfaced. Auto mode and retry logic read failCount and lastFailReason from the item — if mark_item_failed silently no-ops, those fields are never set, degrading sprint recovery. Fix: remove || true, keep stderr suppression only when source_file is empty (item lookup already failed), and log a warning if update_item returns non-zero.",
      "priority": "should",
      "category": null,
      "steps": [
        "In .aishore/aishore, find mark_item_failed() (around line 1355)",
        "Locate the update_item call with '2>/dev/null || true'",
        "Split the logic: if source_file is empty (item not found), it is expected to fail — log_warn and return 0",
        "Otherwise, call update_item without || true; on failure, emit log_warn 'Failed to record sprint failure for $item_id' but still return 0 (best-effort — do not abort the sprint just because the metadata write failed)",
        "Verify: mark_item_failed on a known item updates failCount; mark_item_failed on an unknown ID logs a warning"
      ],
      "acceptanceCriteria": [
        "mark_item_failed logs a warning when update_item fails instead of silently succeeding",
        "mark_item_failed on a known item correctly sets failCount and lastFailReason",
        "Sprint execution continues even if mark_item_failed cannot write (best-effort semantics preserved)"
      ],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "groomedAt": "2026-03-20",
      "groomingNotes": "|| true suppresses all errors including legitimate write failures. Best-effort semantics should still apply (don't abort sprint on metadata write failure), but we should warn. Split: empty source_file case (item not found = expected, keep silent) vs write failure case (unexpected = log_warn). Low risk, additive warning only.",
      "completedAt": "2026-03-20T13:15:00-07:00"
    },
    {
      "id": "BUG-024",
      "title": "backlog add --ac-verify missing validate_arg allows empty verify command",
      "intent": "backlog add must reject --ac-verify without a value — an empty verify command silently creates a broken AC entry that fails at sprint verification time with no clear error.",
      "description": "In cmd_backlog_add(), all value-bearing flags call validate_arg except --ac-verify (lines 3652-3659). The companion cmd_backlog_edit() correctly calls validate_arg for --ac-verify at line 3915. Without the guard, 'backlog add --ac \"check it\" --ac-verify' (no value) either causes an unbound variable crash under set -u or silently creates {\"text\":\"check it\",\"verify\":\"\"} with an empty verify command. At sprint time, the empty verify command fails with a confusing error rather than at edit time where the fix is obvious.",
      "priority": "should",
      "category": null,
      "steps": [
        "In .aishore/aishore, find cmd_backlog_add() and locate the --ac-verify case (around line 3652)",
        "Add validate_arg \"$1\" \"${2:-}\" || return 1 before the if [[ -z \"$pending_ac\" ]] check, matching line 3915 in cmd_backlog_edit()",
        "Verify: 'backlog add --ac \"check\" --ac-verify' (no value) prints error and exits non-zero",
        "Verify: 'backlog add --ac \"check\" --ac-verify \"make test\"' still creates the correct AC entry"
      ],
      "acceptanceCriteria": [
        "backlog add --ac 'text' --ac-verify (no value) exits non-zero and prints an error",
        "backlog add --ac 'text' --ac-verify 'cmd' still works correctly",
        "validate_arg is called for --ac-verify consistent with cmd_backlog_edit"
      ],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "groomedAt": "2026-03-20",
      "groomingNotes": "Missing validate_arg in --ac-verify in cmd_backlog_add is the last validate_arg gap after BUG-017 fixed the others. cmd_backlog_edit already has validate_arg for --ac-verify at line 3915 — add the same call one line before the pending_ac check in cmd_backlog_add. One-line fix, low risk. Steps and AC are concrete.",
      "completedAt": "2026-03-20T13:28:32-07:00"
    },
    {
      "id": "BUG-025",
      "title": "cmd_review --since flag missing validate_arg for empty value",
      "intent": "cmd_review must reject --since without a value — silently consuming the next flag as a commit hash causes the review to run with wrong scope, giving users false confidence the review covered the intended range.",
      "description": "In cmd_review(), the --since flag case at line 2428 does 'since=\"$2\"; shift 2' with no validate_arg call. If the user runs 'aishore review --since' without a commit hash, $2 is either empty or the next flag (e.g. --update-docs). Under set -u, an empty $2 accessed as a positional parameter would cause an unbound variable error; but since $2 is accessed inside a case branch where shift already happened, it just silently sets since to \"\" or the wrong value. All other value-bearing flags across the codebase call validate_arg first. Compare with cmd_backlog_history --since at lines 4019-4025, which correctly validates the value before using it.",
      "priority": "should",
      "category": null,
      "steps": [
        "In .aishore/aishore, find cmd_review() and locate the --since case at line 2428",
        "Add validate_arg \"$1\" \"${2:-}\" || return 1 before the assignment, matching the pattern used in cmd_backlog_edit()",
        "Verify that 'aishore review --since' (no value) now exits non-zero with an error",
        "Verify that 'aishore review --since abc123' and 'aishore review --since abc --update-docs' still work"
      ],
      "acceptanceCriteria": [
        "aishore review --since (no value) exits non-zero with a clear error message",
        "aishore review --since <commit> and aishore review --since --update-docs still work correctly",
        "Error message matches the validate_arg pattern used by other flags"
      ],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "groomedAt": "2026-03-20",
      "groomingNotes": "Same class as BUG-021 (missing validate_arg for value-bearing flag). The cmd_review --since case is the only remaining value-bearing flag without validate_arg in cmd_review. One-line fix. Compare with cmd_backlog_history --since which correctly validates at lines 4019-4025. Low risk, additive only.",
      "completedAt": "2026-03-20T13:31:07-07:00"
    },
    {
      "id": "BUG-026",
      "title": "backlog edit --groomed-at accepts invalid date values without validation",
      "intent": "backlog edit --groomed-at must reject non-date values — silently storing garbage in groomedAt corrupts item metadata and is inconsistent with backlog history --since which validates the same YYYY-MM-DD format.",
      "description": "In cmd_backlog_edit(), the --groomed-at case at lines 3934-3945 accepts any non-empty value that doesn't start with '--' as a valid date. There is no format check. Running 'backlog edit FEAT-001 --groomed-at notadate' silently stores 'notadate' in groomedAt. In contrast, cmd_backlog_history() validates its --since date argument against ^[0-9]{4}-[0-9]{2}-[0-9]{2}$ before using it (lines 4022-4024). The groomedAt field is displayed in backlog show and used for tracking grooming activity, so invalid values undermine tooling built on this field. Fix: add the same regex validation used in cmd_backlog_history() --since.",
      "priority": "could",
      "category": null,
      "steps": [
        "In .aishore/aishore, find cmd_backlog_edit() and locate the --groomed-at case (around line 3934)",
        "After reading gdate from $2, add a format check: if \\! [[ \"$gdate\" =~ ^[0-9]{4}-[0-9]{2}-[0-9]{2}$ ]]; then log_error \"Invalid date format: $gdate (expected YYYY-MM-DD)\"; return 1; fi",
        "This check should only apply when the user provided a value (shift 2 path), not the default-to-today path (shift 1 path)",
        "Verify: 'backlog edit <ID> --groomed-at baddate' errors; 'backlog edit <ID> --groomed-at 2026-01-01' works; 'backlog edit <ID> --groomed-at' sets today's date"
      ],
      "acceptanceCriteria": [
        "backlog edit <ID> --groomed-at notadate exits non-zero and prints an error about invalid date format",
        "backlog edit <ID> --groomed-at 2026-03-20 still sets groomedAt correctly",
        "backlog edit <ID> --groomed-at (no value) still uses today's date as the default",
        "Error message includes expected format: YYYY-MM-DD"
      ],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "groomedAt": "2026-03-20",
      "groomingNotes": "Pattern inconsistency: cmd_backlog_history validates --since date format with ^[0-9]{4}-[0-9]{2}-[0-9]{2}$ at lines 4022-4024; cmd_backlog_edit --groomed-at does no format check. Add the same regex validation in the shift-2 (user-provided value) path only — the default-to-today path is already correct. Low risk, additive. Note: --groomed-at accepts optional value so the validate_arg pattern doesn't apply — must validate after reading.",
      "completedAt": "2026-03-20T13:58:39-07:00"
    },
    {
      "id": "BUG-027",
      "title": "Stash pop fails silently on conflicts after sprint failure",
      "intent": "Users must always know the state of their uncommitted changes. Silent data loss is unacceptable.",
      "description": "",
      "priority": "should",
      "category": null,
      "steps": [
        "Stop suppressing stderr from git stash pop",
        "On pop failure, run git stash show and display output",
        "Use log_warning with actionable guidance",
        "In EXIT trap, apply same visible-failure treatment"
      ],
      "acceptanceCriteria": [
        "When stash pop fails, stderr is visible",
        "log_warning shows stashed files",
        "Warning includes actionable guidance",
        "Normal stash pop still works silently"
      ],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "completedAt": "2026-03-20T15:39:36-07:00"
    },
    {
      "id": "BUG-028",
      "title": "Commit messages are generic — agent summaries discarded",
      "intent": "Every commit message must describe what changed and why. Generic messages make git log useless.",
      "description": "",
      "priority": "must",
      "category": null,
      "steps": [
        "Capture developer agent summary from result.json",
        "Use agent summary as safety-net commit message body",
        "Use agent summary in merge commit message",
        "Fall back to generic message if no summary",
        "Truncate long summaries to 500 chars"
      ],
      "acceptanceCriteria": [
        "Safety-net commit includes developer summary",
        "Merge commit includes agent summary",
        "Falls back to template if no summary",
        "Follows conventional commit format"
      ],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "completedAt": "2026-03-20T15:35:57-07:00"
    },
    {
      "id": "BUG-029",
      "title": "Grooming agents generate unbounded busywork with no stopping condition",
      "intent": "Grooming must converge. Agents must stop when returns diminish.",
      "description": "",
      "priority": "should",
      "category": null,
      "steps": [
        "Add groom_max_items config default 10",
        "Add groom_min_priority config threshold default should",
        "Instruct grooming agent to skip low-value items",
        "Log summary of items added vs skipped",
        "Enforce limits in auto-groom trigger"
      ],
      "acceptanceCriteria": [
        "Grooming stops after groom_max_items per session",
        "Items below groom_min_priority logged but not added",
        "Default cap of 10 items"
      ],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "completedAt": "2026-03-20T15:49:26-07:00"
    },
    {
      "id": "BUG-030",
      "title": "resolve_backlog_file returns wrong file for IDs that don't exist in expected file",
      "intent": "Ensure resolve_backlog_file returns an error when an ID is not found in its expected file, not a stale filename",
      "description": "resolve_backlog_file() uses a prefix case statement: FEAT-* maps to backlog.json, BUG-* maps to bugs.json. It returns the filename immediately without verifying the item actually exists in that file. If find_item then calls jq on the returned filename, it gets an error like 'null' rather than a clear 'item not found'. This is latent: when a FEAT-* item is present only in a custom file, or after backlog clean removes it, resolve_backlog_file returns a stale path and find_item produces a confusing error. The fix is to verify item existence in the resolved file before returning, or at minimum ensure find_item handles the null case gracefully.",
      "priority": "should",
      "category": null,
      "steps": [
        "In .aishore/aishore, find resolve_backlog_file() (around line 520)",
        "For the FEAT-* and BUG-* prefix cases, add a jq existence check after resolving the filename: if jq -e --arg id \"$id\" '.items[] | select(.id == $id)' \"$BACKLOG_DIR/$file\" &>/dev/null; then echo \"$file\"; return 0; fi",
        "Fall through to the generic search-both-files loop if the prefix-matched file doesn't contain the item",
        "Verify: resolve_backlog_file BUG-999 (nonexistent) returns non-zero; resolve_backlog_file BUG-001 still returns bugs.json"
      ],
      "acceptanceCriteria": [
        "resolve_backlog_file returns non-zero for an ID that doesn't exist in any file, even if the prefix matches",
        "resolve_backlog_file still returns the correct file for existing FEAT-* and BUG-* items",
        "find_item error message is 'Item not found: <ID>' not a jq null error when the item is absent"
      ],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "groomedAt": "2026-03-20",
      "groomingNotes": "Latent correctness bug: FEAT-*/BUG-* prefix routing skips existence check. If item was cleaned from backlog, resolve_backlog_file still returns the old filename, causing find_item to fail with a confusing jq error instead of 'item not found'. Fix is additive: add existence check in the prefix case branches before returning, falling through to full search if not found. Low risk, clear scope.",
      "completedAt": "2026-03-20T18:02:49-07:00"
    },
    {
      "id": "BUG-031",
      "title": "cmd_status silently omits in-progress items from sprint readiness display",
      "intent": "cmd_status must show in-progress items clearly so users know a sprint is running and what is being worked on",
      "description": "cmd_status() calls list_ready_items() which filters by readyForSprint: true. Items currently in-progress have readyForSprint set to false by mark_item_failed and related logic, and sprint.json tracks the active sprint separately. But the status output never shows what is currently running — a user who runs 'aishore status' during a sprint sees the item missing from the ready list with no explanation. The status command should query sprint.json and display the active sprint item ID and title if a sprint is in progress.",
      "priority": "should",
      "category": null,
      "steps": [
        "In .aishore/aishore, find cmd_status() (around line 3114)",
        "After the 'Ready for sprint' section, add a block that reads BACKLOG_DIR/sprint.json",
        "If sprint.json has status \\!= 'idle' and item is non-null, print a 'Currently running' section with item ID and title",
        "Verify: running 'aishore status' during a sprint shows 'Currently running: FEAT-001 — My Feature'; running it with no sprint shows nothing for that section"
      ],
      "acceptanceCriteria": [
        "aishore status shows 'Currently running: <ID> — <title>' when a sprint is active (sprint.json status \\!= idle)",
        "aishore status shows no 'Currently running' section when no sprint is active",
        "In-progress display does not interfere with the Ready for sprint section"
      ],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "groomedAt": "2026-03-20",
      "groomingNotes": "UX gap: 'aishore status' is the go-to command for checking system state, but it shows nothing about an actively running sprint. Users running status mid-sprint see an empty ready list with no explanation — they can't tell if a sprint is running or if something is wrong. sprint.json already tracks the active item; this is a display-only change in cmd_status(). Low risk, additive.",
      "completedAt": "2026-03-20T18:04:57-07:00"
    },
    {
      "id": "BUG-032",
      "title": "backlog history output omits duration column when duration is 0",
      "intent": "backlog history must display duration consistently — missing duration for items completed outside normal sprint flow is confusing and breaks column alignment",
      "description": "cmd_backlog_history() formats the output table with ID, date, status, and duration columns. The duration field in sprints.jsonl is stored as an integer (seconds). When an item is archived without a valid startedAt timestamp (e.g. manually marked done, or archive written before sprint started), duration is 0. The display logic should show '0s' or '-' for zero-duration entries rather than silently omitting the column or showing raw '0', which breaks column alignment for scripting.",
      "priority": "could",
      "category": null,
      "steps": [
        "In .aishore/aishore, find cmd_backlog_history() (around line 4550)",
        "Locate the jq format expression that renders the duration column",
        "Change the duration display: if duration is 0 or null, show '-'; otherwise format as minutes and seconds (e.g. '2m 34s' for 154)",
        "Verify: 'backlog history' shows '-' for items with duration 0 and formatted time for items with duration > 0"
      ],
      "acceptanceCriteria": [
        "backlog history shows '-' for items where duration is 0 or null",
        "backlog history formats non-zero duration as human-readable time (e.g. '2m 34s')",
        "Column alignment is consistent across all rows"
      ],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "groomedAt": "2026-03-20",
      "groomingNotes": "Display bug in backlog history: duration=0 produces '0' in the table which looks wrong and breaks column alignment. Items manually marked done or archived before sprint started all have duration=0. Fix is display-only: show '-' for zero/null, format seconds as Xm Ys for non-zero. Low risk.",
      "completedAt": "2026-03-20T18:10:11-07:00"
    },
    {
      "id": "BUG-033",
      "title": "backlog add --scope flag missing in cmd_backlog_add — scope can only be set via edit",
      "intent": "Users must be able to set scope constraints at item creation time — requiring an extra edit command after add is an unnecessary workflow friction that will be missed.",
      "description": "cmd_backlog_add() does not have a --scope flag. The scope array (glob patterns constraining where the developer agent may create or modify files) can only be set by running 'backlog edit <ID> --scope' after the item is created. This is inconsistent with --ac and --step which are both supported at add-time. The fix follows the same pattern as --ac and --step: add --scope and --clear-scope flags to cmd_backlog_add that accumulate scope patterns into a local array and write them as the scope field.",
      "priority": "should",
      "category": null,
      "steps": [
        "In .aishore/aishore, find cmd_backlog_add() and locate the --ac and --step flag handling as reference (around line 3617-3660)",
        "Add a local scope_entries=() array before the flag parsing loop",
        "Add a --scope flag case: validate_arg \"$1\" \"${2:-}\" || return 1; scope_entries+=(\"$2\"); shift 2",
        "After the flag loop, build scope_json from scope_entries array: scope_json='['; for e in \"${scope_entries[@]:-}\"; do scope_json+=\"\\\"$e\\\",\"; done; scope_json='${scope_json%,}]'",
        "Pass --argjson scope_arr \"$scope_json\" to the add_item jq call and include .scope: $scope_arr in the item structure",
        "Verify: 'backlog add --title X --scope \"src/**\" --scope \"tests/**\"' creates item with scope: [\"src/**\",\"tests/**\"]"
      ],
      "acceptanceCriteria": [
        "backlog add --scope 'src/**' creates an item with scope: [\"src/**\"]",
        "backlog add --scope 'src/**' --scope 'tests/**' creates scope with both patterns in order",
        "backlog add with no --scope flag creates item with scope: [] (unchanged default)"
      ],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "groomedAt": "2026-03-20",
      "groomingNotes": "Missing --scope flag at add-time is a workflow gap — users must run two commands (add + edit) to create a scope-constrained item. The pattern already exists: --ac and --step both follow the accumulate-to-array then write-as-json approach. Scope follows the same pattern. Low risk, additive only.",
      "completedAt": "2026-03-20T18:07:57-07:00"
    },
    {
      "id": "BUG-034",
      "title": "next_id scans sprints.jsonl with per-line jq — slow on large archives",
      "intent": "ID generation must remain fast as the sprint archive grows — per-line jq invocations in a shell loop are O(n) and will become noticeable at hundreds of entries.",
      "description": "In next_id(), the archive max-ID computation uses 'jq -r ... archive_file | sort -rn | head -1'. This invokes jq once on the whole JSONL file, which is correct. However the BACKLOG_FILES loop calls jq once per file, and the archive jq reads all lines just to get the max numeric suffix. For large archives (hundreds of sprint entries), this is still fast, but the JSONL slurp approach (jq -r '[...] | max') is more robust than relying on sort -rn with potentially non-numeric values. The real risk is that if a non-numeric suffix appears in itemId (e.g. 'FEAT-new'), jq tonumber throws an error that is silently swallowed by 2>/dev/null, hiding ID conflicts. Fix: add error handling and filter to numeric-only suffixes before max.",
      "priority": "could",
      "category": null,
      "steps": [
        "In .aishore/aishore, find next_id() (around line 652)",
        "In the archive scan, change the jq pipeline to filter to numeric IDs only: jq -r --arg pfx \"$prefix-\" '[.itemId | select(startswith($pfx)) | ltrimstr($pfx) | select(test(\"^[0-9]+$\")) | tonumber] | max // 0'",
        "Apply the same numeric-only filter in the BACKLOG_FILES loop: add | select(test(\"^[0-9]+$\")) before tonumber",
        "Verify: next_id FEAT still returns FEAT-023 (or correct next ID) with numeric items; non-numeric IDs in archive are silently skipped"
      ],
      "acceptanceCriteria": [
        "next_id correctly skips non-numeric ID suffixes without crashing",
        "next_id still returns the correct next sequential ID for FEAT and BUG prefixes",
        "No regression in ID generation for existing items"
      ],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "groomedAt": "2026-03-20",
      "groomingNotes": "Correctness issue: non-numeric ID suffixes cause tonumber to throw in jq, silently swallowed by 2>/dev/null, potentially causing ID conflicts. Fix is additive: add select(test(\"^[0-9]+$\")) before tonumber in both the BACKLOG_FILES loop and archive scan. Low risk, small scope.",
      "completedAt": "2026-03-20T18:13:09-07:00"
    },
    {
      "id": "BUG-035",
      "title": "collect_done_ids misses archived items — cleaned items cause false unmet-dependency warnings",
      "intent": "Ensure dependency resolution accounts for items removed via 'clean' — a sprint item whose dependency was completed and then cleaned must not be blocked.",
      "description": "collect_done_ids() only scans the active backlog files (backlog.json and bugs.json) for items with status=done. When a completed item is removed via 'aishore clean' or 'backlog rm', its ID disappears from both backlogs. Any item that depends on the cleaned item will now show an unmet dependency warning during pick_item and check_readiness_gates, even though the dependency WAS completed. The archive (sprints.jsonl) contains all completed sprint IDs and is the correct source of truth for historical completions. collect_done_ids() should also scan sprints.jsonl to include archived item IDs in the done set.",
      "priority": "should",
      "category": null,
      "steps": [
        "Identify the collect_done_ids() function in .aishore/aishore",
        "After collecting done IDs from active backlog files, also scan ARCHIVE_DIR/sprints.jsonl for all itemId values and add them to the done_ids set",
        "Use jq to extract itemId values from sprints.jsonl (one JSON object per line) and merge with the existing done_ids array, deduplicating",
        "Verify the change does not break items that are both in the active backlog (status=done) and in the archive — deduplication handles this",
        "Test: add item B that depends on item A, complete item A, run 'aishore clean', then pick_item should still treat A's dependency as satisfied for B"
      ],
      "acceptanceCriteria": [
        "pick_item no longer shows unmet-dependency warnings for items whose dependency has been completed and then cleaned via 'aishore clean'",
        "check_readiness_gates no longer reports missing dependency warnings for items whose dependency was archived",
        "collect_done_ids includes IDs from sprints.jsonl in addition to IDs from active backlog files",
        "No regression: items with truly unmet dependencies (never completed) still produce warnings"
      ],
      "scope": [
        ".aishore/aishore"
      ],
      "dependsOn": [],
      "status": "todo",
      "passes": false,
      "readyForSprint": true,
      "groomedAt": "2026-03-20",
      "groomingNotes": "Real bug found during code review. collect_done_ids only reads active backlogs, but clean removes done items from there. sprints.jsonl is the authoritative record. Fix is surgical and low-risk. AC are testable. Priority should is correct.",
      "lastFailReason": "Validator agent rejected implementation",
      "lastFailAt": "2026-03-20T19:14:01-07:00",
      "failCount": 2
    },
    {
      "id": "BUG-036",
      "title": "refine_item_spec JSON extraction fails when agent output has indented or wrapped JSON",
      "intent": "Ensure spec refinement reliably extracts the JSON from the agent's output regardless of indentation or formatting, so retries with spec refinement don't silently fail.",
      "description": "In refine_item_spec(), the JSON extraction from the agent's raw output uses: sed -n '/^\\{/,/^\\}/p'. This only matches a { that starts at column 0, so any indented JSON (e.g. the agent wraps it in a code block with indented content, or the JSON is inside markdown with indentation) will fail to extract. The fallback tries markdown fences but the primary extraction is unreliable. The fix should robustly extract a JSON object that contains both .steps and .acceptanceCriteria keys, using jq itself to validate and extract rather than relying on sed line patterns.",
      "priority": "should",
      "category": null,
      "steps": [
        "Locate refine_item_spec() in .aishore/aishore, specifically the JSON extraction block that uses sed -n '/^\\{/,/^\\}/p'",
        "Replace the fragile sed-based extraction with a jq-based approach: strip markdown fences first, then use jq to find a valid JSON object containing .steps and .acceptanceCriteria",
        "Use: json_output=$(echo \"$raw_output\" | sed '/^```/d' | jq -s 'first(.[] | objects | select(.steps and .acceptanceCriteria))' 2>/dev/null) as the primary extraction",
        "Keep the existing validation check: if ! echo \"$json_output\" | jq -e '.steps and .acceptanceCriteria' — but only if json_output is non-empty",
        "Log a clear error if extraction still fails after the improved approach",
        "Test: verify refinement works when agent wraps JSON in a markdown code block (common output format)"
      ],
      "acceptanceCriteria": [
        "refine_item_spec succeeds when the agent returns JSON indented inside a markdown code block",
        "refine_item_spec succeeds when the agent returns JSON at top-level with no indentation",
        "refine_item_spec logs 'Refinement produced no valid JSON' and returns 1 when the agent returns no JSON object",
        "No regression in the existing fallback path for markdown-fenced JSON"
      ],
      "scope": [
        ".aishore/aishore"
      ],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "groomedAt": "2026-03-20",
      "groomingNotes": "Real bug: sed '/^\\{/,/^\\}/p' pattern only matches JSON starting at column 0. Claude commonly outputs JSON inside markdown code blocks or with leading spaces. The fix uses jq itself for extraction which is far more robust. Priority should — refine is a recovery path, failures here mean no spec recovery on exhausted retries.",
      "completedAt": "2026-03-20T18:24:22-07:00"
    },
    {
      "id": "BUG-037",
      "title": "backlog history output missing title column — makes history hard to read",
      "intent": "Ensure backlog history output includes the item title so users can read the history without cross-referencing backlog list.",
      "description": "cmd_backlog_history outputs a table with columns: ID, DATE, STATUS, DURATION. It does not include the item title. The archive (sprints.jsonl) does not store the title — it only stores itemId, date, status, duration, filesChanged, linesAdded, linesRemoved, priority, category. To show a title, the command would need to look it up in the active backlog. However, since items may have been cleaned out, the title may not always be available. The fix should: (1) attempt to look up the title from active backlogs, and (2) fall back to showing the itemId if the item has been cleaned. This makes history scannable without requiring a separate backlog list lookup.",
      "priority": "should",
      "category": null,
      "steps": [
        "In cmd_backlog_history(), after collecting the jq results, join each itemId against the active backlogs to retrieve the title",
        "Build a lookup map of id->title from both backlog.json and bugs.json using jq before printing the table",
        "Add a TITLE column to the printf format string in the table header and the formatted output",
        "When a title is not found in active backlogs (item was cleaned), fall back to displaying the itemId itself or '-' in the title column",
        "Adjust column widths: ID 12, DATE 12, STATUS 10, DURATION 10, TITLE remainder",
        "Test: run backlog history on a repo with both active and cleaned items to verify titles are shown correctly"
      ],
      "acceptanceCriteria": [
        "backlog history output includes a TITLE column showing the item's title from the active backlog",
        "When an item has been cleaned from the backlog, history shows '-' or the itemId in the title column rather than failing",
        "Column headers are aligned: ID, DATE, STATUS, DURATION, TITLE",
        "Existing --since, --failed, --limit flags still work correctly with the updated output"
      ],
      "scope": [
        ".aishore/aishore"
      ],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "groomedAt": "2026-03-20",
      "groomingNotes": "UX bug: backlog history is barely readable without titles. The archive doesn't store titles so we need to look them up from active backlogs, with fallback for cleaned items. Title lookup is a join on two small JSON files — cheap. Priority should.",
      "completedAt": "2026-03-20T18:27:25-07:00"
    },
    {
      "id": "BUG-038",
      "title": "_check_groom_progress uses md5sum which fails on macOS without coreutils",
      "intent": "Ensure groom progress monitoring works on macOS by using a portable hashing command, so backlog change detection during grooming is not broken for macOS users.",
      "description": "In _check_groom_progress(), backlog file changes are detected using md5sum (lines 958 and 969 in the current codebase). md5sum is a GNU coreutils command that is not available on macOS by default. macOS provides 'md5' (different interface) or 'shasum'/'sha256sum' (different output format). When md5sum is missing, the command silently fails (cur_hash will be empty) and no changes are ever detected, so the progress monitor never reports backlog updates during groom sessions. The fix should use the same portability pattern as verify_checksum() — try sha256sum first, then shasum, then fall back to a simple wc-c checksum if neither is available.",
      "priority": "should",
      "category": null,
      "steps": [
        "Locate _check_groom_progress() in .aishore/aishore and find the two md5sum calls that compute cur_hash",
        "Replace both md5sum calls with a portable hash helper that tries sha256sum, then shasum -a 256, then cksum as a last resort",
        "Define a local _file_hash function or inline the portable hash command using the same approach as verify_checksum(): if command -v sha256sum, elif command -v shasum, else cksum",
        "Verify the replacement produces a stable hash for the same file content (cksum is acceptable as a fallback since we only need change detection, not cryptographic security)",
        "Test on macOS: run 'aishore groom' and confirm that backlog update messages are printed during the session"
      ],
      "acceptanceCriteria": [
        "_check_groom_progress correctly detects backlog file changes on macOS when only md5 or shasum is available (not md5sum)",
        "_check_groom_progress correctly detects backlog file changes on Linux where sha256sum or md5sum are available",
        "No regression: progress messages are still printed when backlog files change during groom sessions",
        "The portable hash approach does not generate errors or warnings when md5sum is absent"
      ],
      "scope": [
        ".aishore/aishore"
      ],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "groomedAt": "2026-03-20",
      "groomingNotes": "Real portability bug: md5sum is GNU-only. macOS users get silent failure in the groom progress monitor. The fix is the same portability pattern already used in verify_checksum(). Priority should — macOS is a primary development platform for Claude Code users.",
      "completedAt": "2026-03-20T18:29:45-07:00"
    },
    {
      "id": "BUG-039",
      "title": "backlog edit --depends-on replaces entire dependsOn array instead of appending like --step and --ac",
      "intent": "Ensure --depends-on on backlog edit is consistent with --step and --ac: multiple calls should append dependencies, not replace the entire list each time.",
      "description": "In cmd_backlog_edit(), the --depends-on flag builds a deps_json array from the provided value and uses '.dependsOn = $deps' which replaces the entire dependsOn array. By contrast, --step uses '.steps = ((.steps // []) + $newsteps)' and --ac uses '.acceptanceCriteria = ((.acceptanceCriteria // []) + $newac)' — both append. This inconsistency means that if you add a dependency incrementally (e.g., two separate calls to add two dependencies), the second call wipes out the first. A user who wants to add one dependency at a time must always specify the full list. The fix should make --depends-on append to the existing dependsOn array, matching the behavior of --step and --ac. A separate --clear-depends-on flag should be added to allow full replacement when needed.",
      "priority": "could",
      "category": null,
      "steps": [
        "In cmd_backlog_edit(), find the --depends-on case that builds deps_json and sets .dependsOn = $deps",
        "Change the jq expression from '.dependsOn = $deps' to '.dependsOn = ((.dependsOn // []) + $deps) | unique'",
        "Add a --clear-depends-on flag that sets '.dependsOn = []', consistent with --clear-steps and --clear-scope",
        "Update the usage/help string in cmd_backlog_edit to mention --clear-depends-on",
        "Test: run backlog edit ID --depends-on A, then backlog edit ID --depends-on B, and verify both A and B appear in dependsOn"
      ],
      "acceptanceCriteria": [
        "Running 'backlog edit ID --depends-on A' followed by 'backlog edit ID --depends-on B' results in dependsOn=[A, B]",
        "Running 'backlog edit ID --depends-on A,B' in a single call also works correctly",
        "Running 'backlog edit ID --clear-depends-on' clears the dependsOn array to []",
        "No regression: existing --depends-on behavior for first-time setting still works"
      ],
      "scope": [
        ".aishore/aishore"
      ],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "groomedAt": "2026-03-20",
      "groomingNotes": "API inconsistency: --step and --ac append, but --depends-on replaces. Fix is straightforward jq change. Adding --clear-depends-on completes the pattern. Priority could — less common operation than steps/AC but inconsistency is a footgun.",
      "completedAt": "2026-03-20T18:34:11-07:00"
    },
    {
      "id": "BUG-040",
      "title": "Extract run_validated_command() helper — validation execution duplicated 3 times",
      "intent": "Eliminate the 3 copies of timeout-aware validation command execution (pre-flight, main validation, AC verification) by extracting a single run_validated_command() helper function",
      "description": "",
      "priority": "should",
      "category": null,
      "steps": [
        "Find all 3 instances of the timeout/bash -c VALIDATE_CMD pattern (pre-flight, main validation, AC verification)",
        "Extract a run_validated_command() function that takes a label and output file, handles timeout vs gtimeout detection, and runs the command",
        "Replace all 3 call sites with calls to the new helper",
        "Run shellcheck and verify no regressions"
      ],
      "acceptanceCriteria": [
        "Only one function contains the timeout-aware bash -c VALIDATE_CMD pattern",
        "All 3 original call sites use the new helper",
        "shellcheck passes"
      ],
      "scope": [],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "completedAt": "2026-03-20T19:17:00-07:00"
    },
    {
      "id": "BUG-041",
      "title": "Cache timeout command detection at startup instead of repeated runtime checks",
      "intent": "Detect timeout vs gtimeout once at startup and store in TIMEOUT_CMD variable, eliminating repeated command -v checks scattered through the script",
      "description": "",
      "priority": "could",
      "category": null,
      "steps": [
        "Add TIMEOUT_CMD detection near other startup constants after set -euo pipefail",
        "Replace all inline command -v timeout and command -v gtimeout checks with usage of TIMEOUT_CMD",
        "Verify gtimeout fallback still works on macOS"
      ],
      "acceptanceCriteria": [
        "No command -v timeout or command -v gtimeout checks remain outside the single detection point",
        "shellcheck passes"
      ],
      "scope": [],
      "dependsOn": [],
      "status": "todo",
      "passes": false,
      "readyForSprint": true
    },
    {
      "id": "BUG-042",
      "title": "Extract require_numeric_arg() helper — numeric validation duplicated for multiple flags",
      "intent": "Replace the 3+ copies of numeric argument validation (regex check, log error, return 1) with a single require_numeric_arg() helper function",
      "description": "",
      "priority": "could",
      "category": null,
      "steps": [
        "Identify all places that validate numeric CLI arguments with the same if/regex/error pattern",
        "Extract require_numeric_arg() that takes flag name and value, validates, and errors",
        "Replace all call sites"
      ],
      "acceptanceCriteria": [
        "Only one function contains the numeric argument validation logic",
        "shellcheck passes"
      ],
      "scope": [],
      "dependsOn": [],
      "status": "todo",
      "passes": false,
      "readyForSprint": true
    },
    {
      "id": "BUG-043",
      "title": "Decompose _run_retry_loop() into focused sub-functions",
      "intent": "Break the 167-line _run_retry_loop() into 3-4 focused functions (run_preflight, run_developer_cycle, run_validation_cycle) so each does one thing and is independently testable",
      "description": "",
      "priority": "should",
      "category": null,
      "steps": [
        "Extract run_preflight_validation() — baseline validation before developer runs",
        "Extract run_developer_cycle() — developer agent invocation with fix command",
        "Extract run_validation_cycle() — scope check, validation cmd, AC verify, validator agent",
        "Refactor _run_retry_loop to orchestrate the extracted functions",
        "Verify sprint execution still works end-to-end"
      ],
      "acceptanceCriteria": [
        "_run_retry_loop is under 60 lines",
        "Each extracted function has a single clear responsibility",
        "shellcheck passes"
      ],
      "scope": [],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "completedAt": "2026-03-20T19:20:20-07:00"
    },
    {
      "id": "BUG-044",
      "title": "Decompose cmd_run() into focused sub-functions",
      "intent": "Break the 477-line cmd_run() into 5-6 focused functions separating arg parsing, git operations, sprint loop, auto-groom logic, and summary reporting",
      "description": "",
      "priority": "should",
      "category": null,
      "steps": [
        "Extract cmd_run_parse_args() for option parsing",
        "Extract setup_sprint_environment() for lock, stash, worktree, config loading",
        "Extract run_sprint_loop() for the core pick-implement-validate-merge cycle",
        "Extract print_sprint_summary() for end-of-run reporting",
        "Refactor cmd_run to orchestrate the extracted functions",
        "Verify sprint execution still works end-to-end"
      ],
      "acceptanceCriteria": [
        "cmd_run is under 80 lines",
        "Each extracted function has a single clear responsibility",
        "shellcheck passes"
      ],
      "scope": [],
      "dependsOn": [
        "BUG-043"
      ],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "completedAt": "2026-03-20T19:27:20-07:00"
    },
    {
      "id": "BUG-045",
      "title": "Consolidate duplicate item filtering logic between pick_item() and list_pickable_ids()",
      "intent": "Eliminate the 95% overlapping jq filters in pick_item() and list_pickable_ids() by extracting a shared pickable-items jq filter or helper function",
      "description": "",
      "priority": "should",
      "category": null,
      "steps": [
        "Compare the jq filters in pick_item() and list_pickable_ids() to identify exact overlap",
        "Extract a shared jq filter variable or helper function for pickable item selection",
        "Refactor both functions to use the shared filter",
        "Verify item picking still works correctly with dependencies and priority ordering"
      ],
      "acceptanceCriteria": [
        "The core filtering logic (ready, intent, dependencies, skip list) exists in exactly one place",
        "Both pick_item and list_pickable_ids produce identical results to before",
        "shellcheck passes"
      ],
      "scope": [],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "completedAt": "2026-03-20T19:31:13-07:00"
    },
    {
      "id": "BUG-046",
      "title": "Replace global side-effect variables with explicit function returns",
      "intent": "Eliminate CHECK_GATES_WARNINGS, SCOPE_VIOLATION_REPORT, AC_VERIFY_REPORT, and GROOM_ITEMS_ADDED/SKIPPED globals by having functions return results via stdout capture instead of global mutation",
      "description": "",
      "priority": "should",
      "category": null,
      "steps": [
        "Refactor check_readiness_gates() to output warnings to stdout instead of setting CHECK_GATES_WARNINGS",
        "Refactor check_scope_violations() to output report to stdout instead of setting SCOPE_VIOLATION_REPORT",
        "Refactor run_ac_verification() to output report to stdout instead of setting AC_VERIFY_REPORT",
        "Refactor enforce_groom_limits() to return counts via stdout instead of global GROOM_ITEMS_ADDED/SKIPPED",
        "Update all callers to capture output instead of reading globals"
      ],
      "acceptanceCriteria": [
        "No CHECK_GATES_WARNINGS, SCOPE_VIOLATION_REPORT, AC_VERIFY_REPORT, GROOM_ITEMS_ADDED, or GROOM_ITEMS_SKIPPED global variables exist",
        "All callers capture function output explicitly",
        "shellcheck passes"
      ],
      "scope": [],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "completedAt": "2026-03-20T20:03:58-07:00"
    },
    {
      "id": "BUG-047",
      "title": "Standardize error handling — functions log+return, only main exits",
      "intent": "Establish and enforce a consistent error handling convention: all functions use log_error + return 1 (never exit 1), only the main entry point and truly fatal pre-flight checks may call exit",
      "description": "",
      "priority": "should",
      "category": null,
      "steps": [
        "Audit all exit 1 calls in functions (not main) and determine which should be return 1",
        "Convert inappropriate exit 1 calls to return 1 with proper log_error",
        "Add log_error to any bare return 1 calls that silently fail",
        "Document the convention in a comment near the error handling functions"
      ],
      "acceptanceCriteria": [
        "No function other than main entry point or pre-flight checks uses exit 1",
        "Every return 1 is preceded by a log_error call",
        "shellcheck passes"
      ],
      "scope": [],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "completedAt": "2026-03-20T20:17:38-07:00"
    },
    {
      "id": "BUG-048",
      "title": "Extract shared backlog iteration helper — for-loop-over-BACKLOG_FILES pattern repeated 10+ times",
      "intent": "Replace the 10+ copies of the for f in BACKLOG_FILES / jq operation pattern with reusable helpers like map_backlog_files() and sum_backlog_field()",
      "description": "",
      "priority": "should",
      "category": null,
      "steps": [
        "Identify all instances of the for f in BACKLOG_FILES iteration pattern",
        "Categorize by usage: summing counts, finding items, transforming items",
        "Extract sum_backlog_field() for count aggregation across files",
        "Extract find_in_backlogs() for item lookup across files",
        "Replace all call sites with the appropriate helper"
      ],
      "acceptanceCriteria": [
        "The raw for f in BACKLOG_FILES pattern appears in at most 2 places (the helpers themselves)",
        "shellcheck passes"
      ],
      "scope": [],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "completedAt": "2026-03-20T20:27:13-07:00"
    },
    {
      "id": "BUG-049",
      "title": "Extract shared option parser to reduce duplication across cmd_* functions",
      "intent": "Replace the duplicated while/case/shift option parsing in every cmd_* function with a shared parse_opts() helper or standardized pattern, eliminating ~100 lines of boilerplate",
      "description": "",
      "priority": "could",
      "category": null,
      "steps": [
        "Catalog all cmd_* functions and their option parsing patterns",
        "Design a shared parse_opts() helper or declare-based pattern",
        "Refactor cmd_run, cmd_backlog_add, cmd_backlog_edit, cmd_backlog_list and others to use it",
        "Ensure unknown option rejection still works per-command"
      ],
      "acceptanceCriteria": [
        "Option parsing boilerplate is reduced by at least 50%",
        "All commands still correctly parse their flags and reject unknown options",
        "shellcheck passes"
      ],
      "scope": [],
      "dependsOn": [],
      "status": "todo",
      "passes": false,
      "readyForSprint": true
    },
    {
      "id": "BUG-050",
      "title": "Replace JSON array string concatenation with jq -s in backlog add",
      "intent": "Replace the manual JSON array construction via string concatenation in a loop (ac_json building) with proper jq -s array construction to avoid potential escaping issues",
      "description": "",
      "priority": "could",
      "category": null,
      "steps": [
        "Find the string concatenation pattern that builds ac_json with manual comma insertion",
        "Replace with printf and jq -s . to build the array safely",
        "Verify AC entries with special characters (quotes, newlines) are handled correctly"
      ],
      "acceptanceCriteria": [
        "No manual JSON array construction via string concatenation exists in the codebase",
        "AC entries with quotes and special characters round-trip correctly",
        "shellcheck passes"
      ],
      "scope": [],
      "dependsOn": [],
      "status": "todo",
      "passes": false,
      "readyForSprint": true
    },
    {
      "id": "BUG-051",
      "title": "Reduce double jq parsing — extract all needed fields in one pass",
      "intent": "Eliminate the pattern of extracting a jq projection then re-parsing the result for individual fields, by extracting all needed fields in a single jq pass using @base64 or multi-output",
      "description": "",
      "priority": "could",
      "category": null,
      "steps": [
        "Identify all places where jq output is captured then re-parsed with another jq call",
        "Consolidate into single jq calls that extract all needed fields at once",
        "Use read or IFS-based splitting for multiple field extraction"
      ],
      "acceptanceCriteria": [
        "No instance of echo item_json pipe jq for single field extraction after a prior jq projection",
        "shellcheck passes"
      ],
      "scope": [],
      "dependsOn": [],
      "status": "todo",
      "passes": false,
      "readyForSprint": true
    },
    {
      "id": "BUG-052",
      "title": "Clean up unused and redundant variable declarations",
      "intent": "Remove or properly scope variables that are declared globally but used locally (LAST_AGENT_LOG, GROOM_ITEMS_ADDED/SKIPPED init), fix the file_count/line_count naming bug, and clean up mid-file global declarations",
      "description": "",
      "priority": "could",
      "category": null,
      "steps": [
        "Move LAST_AGENT_LOG to local scope in the functions that use it",
        "Remove redundant global initialization of GROOM_ITEMS_ADDED/SKIPPED (already reset inside function)",
        "Fix variable named file_count that should be line_count in truncate_sprints_jsonl",
        "Consolidate all remaining global variable declarations to the top of the script"
      ],
      "acceptanceCriteria": [
        "No global variables are declared mid-file (all at top or inside functions as local)",
        "The file_count/line_count naming mismatch is fixed",
        "shellcheck passes"
      ],
      "scope": [],
      "dependsOn": [],
      "status": "todo",
      "passes": false,
      "readyForSprint": true
    },
    {
      "id": "BUG-053",
      "title": "Use printf instead of echo for variable output throughout the script",
      "intent": "Replace ~20 instances of echo with variable arguments with printf to avoid issues when variables start with dash or contain escape sequences, following POSIX best practices",
      "description": "",
      "priority": "could",
      "category": null,
      "steps": [
        "Find all echo calls that output variable content (not static strings)",
        "Replace with printf '%s\\n' for simple variable output",
        "Leave echo for static string literals which are safe"
      ],
      "acceptanceCriteria": [
        "No echo call outputs an unquoted or potentially-dash-prefixed variable",
        "shellcheck passes"
      ],
      "scope": [],
      "dependsOn": [],
      "status": "todo",
      "passes": false,
      "readyForSprint": true
    },
    {
      "id": "BUG-054",
      "title": "Hardcoded safe fields in protect_items_from_groom() should be a declared constant",
      "intent": "Extract the hardcoded safe fields list in protect_items_from_groom() into a named constant array so the merge logic is transparent and maintainable",
      "description": "",
      "priority": "could",
      "category": null,
      "steps": [
        "Find the hardcoded safe fields comparison in protect_items_from_groom()",
        "Extract to a GROOM_SAFE_FIELDS constant array declared with other constants",
        "Update the jq filter to reference the constant"
      ],
      "acceptanceCriteria": [
        "Safe fields are declared as a named constant, not inline in the jq filter",
        "shellcheck passes"
      ],
      "scope": [],
      "dependsOn": [],
      "status": "todo",
      "passes": false,
      "readyForSprint": true
    },
    {
      "id": "BUG-055",
      "title": "Duplicate CLI commands section in groom agent prompts — factor out shared preamble",
      "intent": "Eliminate the ~8 lines of duplicated CLI command reference between product-owner.md and tech-lead.md by extracting a shared section or having the orchestrator inject it",
      "description": "",
      "priority": "could",
      "category": null,
      "steps": [
        "Compare the CLI commands sections in product-owner.md and tech-lead.md",
        "Extract shared content to a common block injected by build_agent_prompt() in the orchestrator",
        "Keep role-specific differences (--type feat vs --type bug) in the individual prompts"
      ],
      "acceptanceCriteria": [
        "No duplicated CLI command text exists across agent prompt files",
        "Both groom agents still receive full CLI command guidance"
      ],
      "scope": [],
      "dependsOn": [],
      "status": "todo",
      "passes": false,
      "readyForSprint": true
    },
    {
      "id": "BUG-056",
      "title": "Update CHANGELOG.md with v0.3.3 release notes",
      "intent": "Document all changes since v0.3.2 in CHANGELOG.md so the version file and changelog are in sync",
      "description": "",
      "priority": "should",
      "category": null,
      "steps": [
        "Review git log from v0.3.2 tag to HEAD to identify all changes",
        "Write a v0.3.3 section in CHANGELOG.md following the existing format",
        "Verify VERSION file matches the new CHANGELOG entry"
      ],
      "acceptanceCriteria": [
        "CHANGELOG.md has a v0.3.3 section",
        "All commits since v0.3.2 are represented in the changelog"
      ],
      "scope": [],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "completedAt": "2026-03-20T20:31:10-07:00"
    },
    {
      "id": "BUG-057",
      "title": "resolve_backlog_file re-checks expected file in fallback loop — skip it",
      "intent": "When resolve_backlog_file() fails the fast-path check on the expected file, the fallback loop iterates all BACKLOG_FILES including the one already checked, wasting a jq parse — skip it",
      "description": "",
      "priority": "could",
      "category": null,
      "steps": [
        "In resolve_backlog_file(), add a continue condition in the fallback loop when f equals expected",
        "Verify item resolution still works for all edge cases"
      ],
      "acceptanceCriteria": [
        "The fallback loop skips the already-checked expected file",
        "shellcheck passes"
      ],
      "scope": [],
      "dependsOn": [],
      "status": "todo",
      "passes": false,
      "readyForSprint": true
    },
    {
      "id": "BUG-058",
      "title": "Inconsistent date portability — some date calls lack BSD fallback",
      "intent": "Add consistent BSD/GNU date handling throughout the script — some calls have macOS fallback (date -j) while others assume GNU date, which will break on macOS without coreutils",
      "description": "",
      "priority": "could",
      "category": null,
      "steps": [
        "Audit all date command invocations for BSD compatibility",
        "Extract a portable_date() helper that handles both GNU and BSD date",
        "Replace direct date calls with the helper where format conversion is needed"
      ],
      "acceptanceCriteria": [
        "All date calls either use the portable helper or only use universally supported formats like +%s",
        "shellcheck passes"
      ],
      "scope": [],
      "dependsOn": [],
      "status": "todo",
      "passes": false,
      "readyForSprint": true
    },
    {
      "id": "BUG-059",
      "title": "Remove TODO placeholder links from docs/PROBLEMS.md",
      "intent": "Either fill in the evidence links (TODO: Link forum posts, TODO: Link articles) in PROBLEMS.md or remove the placeholder text so the document looks complete",
      "description": "",
      "priority": "could",
      "category": null,
      "steps": [
        "Review all TODO placeholders in PROBLEMS.md",
        "Search for relevant evidence links or remove the placeholders",
        "Ensure the document reads cleanly without dangling TODOs"
      ],
      "acceptanceCriteria": [
        "No TODO placeholders remain in PROBLEMS.md"
      ],
      "scope": [],
      "dependsOn": [],
      "status": "todo",
      "passes": false,
      "readyForSprint": true
    },
    {
      "id": "BUG-060",
      "title": "Extract init scaffold heredocs to template files",
      "intent": "Init templates live in .aishore/templates/ as real files, not heredocs embedded in bash. The aishore script loses ~160 lines and templates become editable without touching code.",
      "description": "Move all heredoc templates from _init_scaffold_files into .aishore/templates/: DEFINITIONS.md, PRODUCT.md.tmpl, ARCHITECTURE.md.tmpl, config.yaml.tmpl, backlog.json, bugs.json. Replace heredocs with cp or sed-based substitution (project_name, validate_cmd). Template files must be added to checksums.sha256 so they are included in updates.",
      "priority": "must",
      "category": null,
      "steps": [
        "Create .aishore/templates/ directory",
        "Extract DEFINITIONS.md heredoc (76 lines) to templates/DEFINITIONS.md",
        "Extract config.yaml heredoc (29 lines) to templates/config.yaml.tmpl with $PROJECT_NAME and $VALIDATE_CMD placeholders",
        "Extract PRODUCT.md heredoc (25 lines) to templates/PRODUCT.md.tmpl with $PROJECT_NAME placeholder",
        "Extract ARCHITECTURE.md heredoc (21 lines) to templates/ARCHITECTURE.md.tmpl with $PROJECT_NAME placeholder",
        "Extract backlog.json heredoc (7 lines) to templates/backlog.json",
        "Extract bugs.json heredoc (6 lines) to templates/bugs.json",
        "Replace heredocs in _init_scaffold_files with cp/sed commands",
        "Add all template files to checksums.sha256",
        "Run .aishore/aishore init -y in a temp dir to verify identical output"
      ],
      "acceptanceCriteria": [
        "All 6 template files exist in .aishore/templates/",
        "_init_scaffold_files uses cp/sed to install templates, contains zero heredocs",
        ".aishore/aishore init -y produces identical directory structure and file contents as before",
        "checksums.sha256 includes all template files",
        "shellcheck .aishore/aishore passes"
      ],
      "scope": [
        ".aishore/**"
      ],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "completedAt": "2026-03-20T19:37:39-07:00"
    },
    {
      "id": "BUG-061",
      "title": "Extract help text to file and kill cmd_usage duplicate",
      "intent": "Help text lives in a file, not inline bash. The duplicate cmd_usage is eliminated. All help entry points produce the same output.",
      "description": "cmd_help (110 lines) and cmd_usage (45 lines) are near-duplicates. Extract cmd_help body to .aishore/help.txt, make cmd_help just cat it with version substitution, and point cmd_usage at cmd_help. Net savings ~150 lines.",
      "priority": "must",
      "category": null,
      "steps": [
        "Extract cmd_help body to .aishore/help.txt with a VERSION placeholder",
        "Replace cmd_help body with: sed to substitute version, then cat",
        "Delete cmd_usage function body, replace with call to cmd_help",
        "Add help.txt to checksums.sha256",
        "Verify all entry points work: help, --help, -h, usage, no-args"
      ],
      "acceptanceCriteria": [
        "help.txt exists in .aishore/ and contains the full help text",
        "cmd_usage function is removed or is a one-line alias to cmd_help",
        ".aishore/aishore help output is identical to before",
        ".aishore/aishore with no args shows help",
        "shellcheck .aishore/aishore passes"
      ],
      "scope": [
        ".aishore/**"
      ],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "completedAt": "2026-03-20T19:42:24-07:00"
    },
    {
      "id": "BUG-062",
      "title": "Move populate-mode docs and claudemd snippet to files",
      "intent": "Static documentation blocks are read from files, not constructed as bash string literals. build_agent_prompt loses ~90 lines of inline text.",
      "description": "build_agent_prompt has ~74 lines of populate-mode documentation inlined as a bash string. get_claudemd_snippet has 15 lines. Move populate docs to the end of agents/product-owner.md (gated by a section header the orchestrator includes only in populate mode). Move claudemd snippet to templates/claude-section.md. Both become cat calls.",
      "priority": "must",
      "category": null,
      "steps": [
        "Append populate-mode documentation to .aishore/agents/product-owner.md under a clear section header",
        "Update build_agent_prompt to conditionally include the populate section from the prompt file instead of injecting it",
        "Extract get_claudemd_snippet heredoc to .aishore/templates/claude-section.md",
        "Replace get_claudemd_snippet body with cat of the template file",
        "Update checksums.sha256",
        "Verify: run a dry-run with populate mode and confirm prompt content is identical"
      ],
      "acceptanceCriteria": [
        "Zero lines of populate documentation exist as bash string literals in aishore",
        "product-owner.md contains the populate-mode guidance",
        "templates/claude-section.md exists and get_claudemd_snippet reads it",
        "Dry-run developer prompt output is unchanged",
        "shellcheck .aishore/aishore passes"
      ],
      "scope": [
        ".aishore/**"
      ],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "completedAt": "2026-03-20T19:49:23-07:00"
    },
    {
      "id": "BUG-063",
      "title": "Remove interactive mode from backlog add",
      "intent": "cmd_backlog_add only accepts flags, no interactive TUI. The function loses ~105 lines. All existing flag-based usage is unchanged.",
      "description": "cmd_backlog_add has ~105 lines of interactive echo/read prompts and a confirmation dialog. This tool is used primarily by AI agents via flags. Remove the interactive code path entirely. When --title is missing, print a usage error with an example command instead of launching a TUI.",
      "priority": "must",
      "category": null,
      "steps": [
        "Remove the interactive mode block (lines checking for missing title and -t 0, through the confirmation dialog)",
        "When title is missing, print a concise usage error with one example and exit 1",
        "Remove the interactive flag from arg parsing",
        "Update cmd_help to remove interactive from backlog add docs",
        "Verify: .aishore/aishore backlog add with title and type flags still works"
      ],
      "acceptanceCriteria": [
        "cmd_backlog_add contains zero interactive read calls",
        "The interactive flag is not accepted",
        "Missing title prints a usage error with example, not a prompt",
        "All flag-based add commands work identically to before",
        "shellcheck passes"
      ],
      "scope": [
        ".aishore/**"
      ],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "completedAt": "2026-03-20T19:52:23-07:00"
    },
    {
      "id": "BUG-064",
      "title": "Simplify AC and step accumulation in backlog add and edit",
      "intent": "All array-building in backlog add and edit uses shared helpers. The pending_ac flush pattern, manual AC JSON string-cat, and triplicated scope/steps/deps append blocks are eliminated. Saves ~50 lines combined.",
      "description": "Three patterns need fixing: (1) pending_ac/flush state machine for --ac/--ac-verify pairing — simplify to push-immediately-on-ac, modify-last-on-ac-verify. (2) AC JSON array built by manual string concatenation (10 identical lines in both add and edit) — extract _build_ac_json helper. (3) Three identical scope/steps/deps append blocks in edit (23 lines) each doing the same if-gt-0/printf/jq-R/jq-s/jq_updates pattern — extract _append_array_field helper. Also remove update_desc tracking from edit (14 lines of string concatenation for a cosmetic log message).",
      "priority": "should",
      "category": null,
      "steps": [
        "Extract _build_ac_json helper that converts ac_entries array to JSON array string",
        "Replace manual AC string-cat in cmd_backlog_add with _build_ac_json call",
        "Replace manual AC string-cat in cmd_backlog_edit with _build_ac_json call",
        "Extract _append_array_field helper: takes field name, bash array, and jq update expression",
        "Replace the 3 scope/steps/deps append blocks in cmd_backlog_edit with _append_array_field calls",
        "Refactor --ac to push immediately; --ac-verify modifies last entry; remove pending_ac",
        "Remove update_desc tracking from cmd_backlog_edit; simplify success message to Updated ID",
        "Verify: add and edit with all flag combinations produce identical JSON output"
      ],
      "acceptanceCriteria": [
        "No pending_ac variable exists in the script",
        "No manual AC JSON string concatenation (first=true loop pattern) exists in the script",
        "A shared _build_ac_json helper is used by both add and edit",
        "A shared _append_array_field helper replaces the 3 identical blocks in edit",
        "backlog add with --ac and --ac-verify produces correct JSON",
        "backlog edit with --ac, --step, --scope, --depends-on produces correct JSON",
        "shellcheck passes"
      ],
      "scope": [
        ".aishore/**"
      ],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "completedAt": "2026-03-20T20:36:23-07:00"
    },
    {
      "id": "BUG-065",
      "title": "Remove backlog sync command",
      "intent": "The backlog sync command is removed entirely. It is a 152-line niche feature that detects manually-completed items by matching scope globs against git history. Nobody uses it and it adds significant weight.",
      "description": "cmd_backlog_sync (152 lines) cross-references item scope globs against git diff to guess which items were completed outside aishore. This is speculative, rarely accurate, and never used in practice. Remove the function, its router entry in cmd_backlog, and its help text entries.",
      "priority": "must",
      "category": null,
      "steps": [
        "Delete cmd_backlog_sync function",
        "Remove sync entry from cmd_backlog router",
        "Remove sync from help text and usage docs",
        "Verify: all other backlog subcommands still work"
      ],
      "acceptanceCriteria": [
        "cmd_backlog_sync function does not exist in the script",
        "Running .aishore/aishore backlog sync prints unknown command error",
        "All other backlog subcommands work: list, add, show, edit, check, rm, history, populate",
        "shellcheck passes"
      ],
      "scope": [
        ".aishore/**"
      ],
      "dependsOn": [],
      "status": "done",
      "passes": true,
      "readyForSprint": false,
      "completedAt": "2026-03-20T19:55:45-07:00"
    },
    {
      "id": "BUG-066",
      "title": "Simplify backlog history to basic listing",
      "intent": "backlog history is a simple dump of the archive, not a query engine. The --since, --failed, and --limit filter flags are removed. The function shrinks from 84 to ~20 lines.",
      "description": "cmd_backlog_history is 84 lines with --since, --failed, --limit flags and a title lookup map. This is overengineered for viewing an append-only JSONL file. Replace with a simple jq format and tail. Users who need filtering can pipe through jq directly.",
      "priority": "should",
      "category": null,
      "steps": [
        "Replace cmd_backlog_history with simple: read sprints.jsonl, format with jq, print last 20",
        "Remove --since, --failed, --limit flag parsing",
        "Keep the formatted table output (ID, date, status, duration, title)",
        "Update help text to remove filter flags"
      ],
      "acceptanceCriteria": [
        "backlog history shows formatted table of recent sprint history",
        "No --since, --failed, or --limit flags are accepted",
        "Output format (columns) is unchanged",
        "shellcheck passes"
      ],
      "scope": [
        ".aishore/**"
      ],
      "dependsOn": [],
      "status": "todo",
      "passes": false,
      "readyForSprint": true
    },
    {
      "id": "BUG-067",
      "title": "Trim groom presentation functions to bare essentials",
      "intent": "Groom summary and diff output is concise status lines, not rich formatted sections. print_groom_summary and print_groom_diff together shrink from 95 to ~25 lines.",
      "description": "print_groom_summary (58 lines) renders a multi-section status with priority breakdowns, highlight lists, and conditional messaging. print_groom_diff (37 lines) shows before/after with multiple computed deltas. Replace both with terse one-liner status output: counts before, counts after, delta.",
      "priority": "should",
      "category": null,
      "steps": [
        "Replace print_groom_summary with 2-3 log_info lines showing item counts and ready counts",
        "Replace print_groom_diff with a single line showing ready before/after and items added/skipped",
        "Remove the has_highlights loop and must_should_total computation from summary",
        "Verify: groom command still shows meaningful before/after context"
      ],
      "acceptanceCriteria": [
        "print_groom_summary is under 15 lines",
        "print_groom_diff is under 12 lines",
        "Groom command output includes item counts and ready counts before and after",
        "shellcheck passes"
      ],
      "scope": [
        ".aishore/**"
      ],
      "dependsOn": [],
      "status": "todo",
      "passes": false,
      "readyForSprint": true
    },
    {
      "id": "BUG-068",
      "title": "Tighten enforce_groom_limits and protect_items_from_groom",
      "intent": "Groom protection and limit enforcement logic is tighter and less verbose. Combined size drops from 149 to ~100 lines while preserving all safety guarantees.",
      "description": "enforce_groom_limits (76 lines) and protect_items_from_groom (73 lines) are correct but verbose. The protection function does per-field comparison with a 6-field jq check then a separate merge — this can be a single jq expression. The limits function iterates items one-by-one in bash when a single jq filter would suffice.",
      "priority": "should",
      "category": null,
      "steps": [
        "Rewrite protect_items_from_groom to use a single jq pass: for each pre-existing ID, replace the live item with the original but carry over safe fields",
        "Rewrite enforce_groom_limits to use jq to filter new items by priority rank and cap count, then remove excess in one pass",
        "Remove per-item bash loops where jq can do the work",
        "Keep all log_warning output for transparency",
        "Verify: groom with a snapshot produces identical protection behavior"
      ],
      "acceptanceCriteria": [
        "protect_items_from_groom is under 50 lines",
        "enforce_groom_limits is under 50 lines",
        "Pre-existing items are never modified by groomer (title, intent, desc, steps, AC preserved)",
        "New items exceeding max count or below min priority are removed",
        "shellcheck passes"
      ],
      "scope": [
        ".aishore/**"
      ],
      "dependsOn": [],
      "status": "todo",
      "passes": false,
      "readyForSprint": true
    },
    {
      "id": "BUG-069",
      "title": "Deduplicate backlog populate with cmd_groom flow",
      "intent": "backlog populate reuses the groom infrastructure instead of duplicating it. The cmd_backlog_populate function shrinks from 83 to ~40 lines by calling shared helpers.",
      "description": "cmd_backlog_populate (83 lines) duplicates the pattern from cmd_groom: acquire lock, load config, find context files, snapshot backlogs, run agent, protect items, enforce limits, print diff. Extract the shared groom/populate flow into a helper that both call with different agent name and context.",
      "priority": "should",
      "category": null,
      "steps": [
        "Extract the common flow (lock, config, snapshot, run agent, protect, enforce limits, diff) into a shared helper function",
        "cmd_groom calls the helper with agent=tech-lead or product-owner and standard context",
        "cmd_backlog_populate calls the helper with agent=product-owner and PRODUCT.md context",
        "Keep populate-specific logic: PRD file discovery, empty template guard",
        "Verify: both groom and populate produce identical behavior"
      ],
      "acceptanceCriteria": [
        "No duplicated snapshot/protect/enforce/diff code between groom and populate",
        "cmd_groom works identically to before",
        "cmd_backlog_populate works identically to before",
        "shellcheck passes"
      ],
      "scope": [
        ".aishore/**"
      ],
      "dependsOn": [],
      "status": "todo",
      "passes": false,
      "readyForSprint": true
    },
    {
      "id": "BUG-070",
      "title": "Consolidate three merge strategies into parameterized flow",
      "intent": "The merge/squash/no-merge paths in _handle_sprint_success share one code flow with a branch only on the merge command itself. Saves ~40 lines of duplicated checkout/push/pull logic.",
      "description": "_handle_sprint_success has three near-identical code blocks for merge, squash, and no-merge. The remote sync block (push + pull + warning) is duplicated verbatim between squash (lines 2132-2138) and no-ff (lines 2162-2168). The merge_msg if/else (lines 2146-2150) is collapsible to one expression. Safety-commit message pattern is also repeated. Parameterize into: (1) no-merge early return with push and optional PR, (2) single merge path that branches only on the git merge command, (3) shared _sync_remote helper for push+pull.",
      "priority": "should",
      "category": null,
      "steps": [
        "Extract _sync_remote helper: git push + git pull with warnings — used by both merge paths",
        "Collapse merge_msg if/else to: merge_msg=merge: ITEM_ID dollar{merge_summary:-sprint branch}",
        "Parameterize merge: single path that runs git merge --no-ff or git merge --squash based on MERGE_STRATEGY",
        "Handle squash-specific explicit commit after merge --squash",
        "Keep no-merge as early return with push and optional PR creation",
        "Verify: all three strategies produce identical git history as before"
      ],
      "acceptanceCriteria": [
        "A _sync_remote helper exists and is called by both merge paths",
        "No duplicated push/pull/cleanup code between merge strategies",
        "merge_msg construction is a single expression, not an if/else block",
        "MERGE_STRATEGY=merge produces --no-ff merge commit",
        "MERGE_STRATEGY=squash produces squash commit",
        "no-merge flag pushes branch and skips merge",
        "shellcheck passes"
      ],
      "scope": [
        ".aishore/**"
      ],
      "dependsOn": [],
      "status": "todo",
      "passes": false,
      "readyForSprint": true
    },
    {
      "id": "BUG-071",
      "title": "Clean up cmd_run: extract auto-review and trim verbosity",
      "intent": "cmd_run is leaner and reads top-to-bottom. The inline auto-review block, verbose bash idioms in the sprint loop, and nested helper definitions are cleaned up. Saves ~65 lines.",
      "description": "cmd_run (478 lines) has: (1) a 68-line auto-review block at the end duplicating cmd_review — replace with direct call. (2) A 4-line skip-list builder loop replaceable with IFS=,; echo. (3) A 13-line priority scope check loop replaceable with a single [[ string match ]]. (4) Verbose startup banner echoing every config value. (5) Nested _count_total_ready and _record_sprint_failure function defs that should be at file scope. (6) Duplicate [[ -s review_output ]] check in post-summary.",
      "priority": "should",
      "category": null,
      "steps": [
        "Replace 68-line inline auto-review block with a direct call to cmd_review",
        "Replace skip-list builder loop with: local skip_list; skip_list=dollar{!FAILED_IDS[*]}",
        "Replace priority scope check loop with: [[ -z auto_priorities || ' auto_priorities ' == *' item_priority '* ]]",
        "Move nested _count_total_ready to file scope",
        "Move nested _record_sprint_failure to file scope",
        "Merge duplicate [[ -s review_output ]] checks into one block",
        "Trim startup banner to essential info only",
        "Verify: auto done --auto-review, auto p1, run --dry-run all work correctly"
      ],
      "acceptanceCriteria": [
        "No inline review agent invocation in cmd_run — it calls cmd_review",
        "No skip-list builder loop — uses IFS one-liner",
        "No priority scope for-loop — uses string match",
        "No nested function definitions inside cmd_run",
        "auto done --auto-review still runs architecture review after completion",
        "shellcheck passes"
      ],
      "scope": [
        ".aishore/**"
      ],
      "dependsOn": [],
      "status": "todo",
      "passes": false,
      "readyForSprint": true
    },
    {
      "id": "BUG-072",
      "title": "Trim verbose defensive coding across all functions",
      "intent": "Error handling is terse and consistent. Redundant null guards, verbose suggestions, unnecessary fallbacks, single-use variables, and redundant return 0 statements are removed. Saves ~200 lines spread across the entire script.",
      "description": "Across 82 functions, the script has accumulated verbose patterns: (1) jq calls with 2>/dev/null || true on caller-guaranteed input. (2) Error messages with full command examples belonging in help text. (3) Existence checks on files just created or caller-validated. (4) Multi-line error blocks where single log_error suffices. (5) ~5 redundant return 0 at end of functions. (6) Per-item dual jq calls in enforce_groom_limits and mark_complete that can be combined into single calls. (7) Variables computed and used exactly once that should be inlined. (8) cmd_diagnose has 6 separate jq -r calls on the same file — consolidate to one.",
      "priority": "should",
      "category": null,
      "steps": [
        "Remove 2>/dev/null from jq calls where input JSON was constructed in the same function",
        "Shorten error messages: remove inline command examples and suggestions",
        "Remove redundant file existence checks where the caller already validated",
        "Collapse multi-line error blocks into single log_error calls",
        "Remove ~5 redundant return 0 at end of functions (load_config, _apply_env_overrides, etc.)",
        "Combine per-item dual jq calls in enforce_groom_limits (id+priority in one call)",
        "Combine two separate jq calls for priority+category in mark_complete into one",
        "Consolidate 6 separate jq -r calls in cmd_diagnose into one jq invocation",
        "Inline single-use variables where the expression is short and clear",
        "Run full test: init, backlog add, backlog list, groom, run --dry-run"
      ],
      "acceptanceCriteria": [
        "No jq call has 2>/dev/null where input JSON was constructed in the same function",
        "No error message contains a full command example longer than 60 characters",
        "No function ends with a redundant return 0",
        "enforce_groom_limits uses one jq call per item, not two",
        "mark_complete uses one jq call for priority+category, not two",
        "cmd_diagnose uses one jq invocation for initial field extraction, not six",
        "All commands still produce correct error output on bad input",
        "shellcheck passes"
      ],
      "scope": [
        ".aishore/**"
      ],
      "dependsOn": [],
      "status": "todo",
      "passes": false,
      "readyForSprint": true
    },
    {
      "id": "BUG-073",
      "title": "Extract _find_prd and unify product/arch detection in init flow",
      "intent": "The PRD candidate list exists in one place. Product and architecture doc paths are detected once, not three times with three variable names. Init flow loses ~35 lines of copy-paste.",
      "description": "The same 10-element PRD candidate list (PRODUCT.md, product.md, PRD.md, prd.md, docs/ variants, WEBSITE_BRIEF.md, README.md) is copy-pasted verbatim in _init_detect_project, _init_scaffold_files, and cmd_backlog_populate. Product/arch existence is checked 3 times across the init flow with 3 different variable names (prd_found, product_found, found_product). Extract _find_prd function returning the path, use it everywhere. Detect product/arch docs once and pass paths through. Remove docs/WEBSITE_BRIEF.md from generic candidate list (project-specific leak).",
      "priority": "should",
      "category": null,
      "steps": [
        "Create _find_prd function that returns the first matching PRD file path from the candidate list",
        "Replace the candidate loop in _init_detect_project with _find_prd call",
        "Replace the candidate loop in _init_scaffold_files with _find_prd call",
        "Replace the candidate loop in cmd_backlog_populate with _find_prd call",
        "Remove docs/WEBSITE_BRIEF.md from the candidate list",
        "Unify product/arch detection: detect once in _init_detect_project, pass paths to scaffold and summary",
        "Remove the re-detection loops in _init_scaffold_files and cmd_init summary",
        "Verify: init -y and backlog populate both find the same docs as before"
      ],
      "acceptanceCriteria": [
        "_find_prd function exists and is called from all 3 locations",
        "No duplicate PRD candidate list exists in the script",
        "docs/WEBSITE_BRIEF.md is not in the candidate list",
        "Product/arch paths are detected once during init, not re-scanned",
        "init -y produces identical output",
        "backlog populate finds the same PRD file as before",
        "shellcheck passes"
      ],
      "scope": [
        ".aishore/**"
      ],
      "dependsOn": [],
      "status": "todo",
      "passes": false,
      "readyForSprint": true
    },
    {
      "id": "BUG-074",
      "title": "Refactor cmd_metrics to compute once, render twice",
      "intent": "Metrics are computed by one jq pass and rendered by two output modes. The 6 duplicated jq computations between JSON and human branches are eliminated. Saves ~12 lines and 6 jq forks.",
      "description": "cmd_metrics (114 lines) computes avg_attempts, items_per_day, by_priority, by_category, and recent-10 trends twice: once in a big jq -s block for --json output, and again as 6 separate jq -s calls for human output. Extract _compute_metrics_json that always produces the JSON object. --json mode emits it directly. Human mode extracts fields from the pre-computed JSON with jq -r.",
      "priority": "should",
      "category": null,
      "steps": [
        "Extract _compute_metrics_json function that returns the full metrics JSON object",
        "cmd_metrics --json: call _compute_metrics_json and emit directly",
        "cmd_metrics human mode: call _compute_metrics_json, then render fields with jq -r extraction",
        "Remove the 6 separate jq -s calls from the human branch",
        "Verify: both --json and human output are identical to before"
      ],
      "acceptanceCriteria": [
        "A _compute_metrics_json function exists",
        "No jq -s call appears in the human-output branch of cmd_metrics",
        "cmd_metrics --json output is identical to before",
        "cmd_metrics human output is identical to before",
        "shellcheck passes"
      ],
      "scope": [
        ".aishore/**"
      ],
      "dependsOn": [],
      "status": "todo",
      "passes": false,
      "readyForSprint": true
    },
    {
      "id": "BUG-075",
      "title": "Collapse sub-shell jq extractions in create_sprint and mark_complete",
      "intent": "Sprint state functions pass item JSON as a single --argjson argument instead of forking 5-7 sub-shells to extract individual fields. Saves ~13 lines and 7 process forks.",
      "description": "create_sprint (44 lines) calls echo item_json | jq 5 separate times to extract intent, description, steps, AC, scope — then passes each as a --argjson to jq -n. Pass item_json as a single --argjson item and reference fields inline. mark_complete (78 lines) calls jq twice to get item_priority and item_category separately from the same backlog file — combine into one call.",
      "priority": "should",
      "category": null,
      "steps": [
        "Refactor create_sprint: pass item_json as --argjson item, reference .intent/.steps/etc inline in the jq expression",
        "Remove the 5 sub-shell extractions in create_sprint",
        "Refactor mark_complete: combine item_priority and item_category into one jq call returning both",
        "Verify: sprint.json and sprints.jsonl archive records are identical to before"
      ],
      "acceptanceCriteria": [
        "create_sprint has zero echo item_json | jq sub-shell calls",
        "create_sprint uses --argjson item to pass the full item",
        "mark_complete uses one jq call for priority+category, not two",
        "sprint.json structure is unchanged",
        "sprints.jsonl archive records are unchanged",
        "shellcheck passes"
      ],
      "scope": [
        ".aishore/**"
      ],
      "dependsOn": [],
      "status": "todo",
      "passes": false,
      "readyForSprint": true
    },
    {
      "id": "BUG-076",
      "title": "Collapse npm detection block in _init_detect_project",
      "intent": "npm validation command detection is 8 lines instead of 22. Three separate jq calls for has_test/has_build/has_check are replaced by a single priority-ordered jq expression.",
      "description": "_init_detect_project has a 22-line block that calls jq 3 times into package.json to extract has_test, has_build, has_check, then uses a 4-way if/elif chain to pick the validation command. Replace with a single jq call that returns the best available script name, then map to the command string.",
      "priority": "could",
      "category": null,
      "steps": [
        "Replace 3 separate jq calls with one jq expression that checks scripts.check, scripts.build, scripts.test in priority order",
        "Map the result to the validation command string (npm run check, npm run build, npm test, etc.)",
        "Remove the has_test/has_build/has_check variables and the 4-way if/elif",
        "Verify: init -y on a node project detects the same validation command as before"
      ],
      "acceptanceCriteria": [
        "npm script detection uses one jq call, not three",
        "No has_test/has_build/has_check variables exist",
        "init -y detects the correct validation command for projects with check, build, test, or combinations",
        "shellcheck passes"
      ],
      "scope": [
        ".aishore/**"
      ],
      "dependsOn": [],
      "status": "todo",
      "passes": false,
      "readyForSprint": true
    },
    {
      "id": "BUG-077",
      "title": "Inline single-use nested helpers in cmd_update",
      "intent": "cmd_update has no single-call-site nested functions. get_expected_checksum and verify_or_abort are inlined into _fetch_and_stage where they are used once each. Saves ~12 lines of wrapper overhead.",
      "description": "cmd_update defines get_expected_checksum (5 lines, called once) and verify_or_abort (21 lines, called once from _fetch_and_stage). Both are nested helpers with no reuse. Inline get_expected_checksum as an awk call at its one use site. Inline verify_or_abort logic into _fetch_and_stage. Also fix misleading Not modified output that lists backlog/* (not in the update manifest).",
      "priority": "could",
      "category": null,
      "steps": [
        "Inline get_expected_checksum: replace call with direct awk expression in _fetch_and_stage",
        "Inline verify_or_abort logic into _fetch_and_stage",
        "Remove the two nested function definitions",
        "Fix Not modified output: remove backlog/* line (backlog is never in the manifest)",
        "Verify: update --dry-run shows correct file list and update installs correctly"
      ],
      "acceptanceCriteria": [
        "No get_expected_checksum function exists in the script",
        "No verify_or_abort function exists in the script",
        "_fetch_and_stage handles checksum lookup and verification inline",
        "Not modified output does not mention backlog",
        "update --dry-run works correctly",
        "shellcheck passes"
      ],
      "scope": [
        ".aishore/**"
      ],
      "dependsOn": [],
      "status": "todo",
      "passes": false,
      "readyForSprint": true
    },
    {
      "id": "BUG-078",
      "title": "Move advisory complexity hints out of check_readiness_gates",
      "intent": "check_readiness_gates is a pure gate function — it checks hard requirements only. Advisory hints (under-specified warnings) live in the caller. Saves ~10 lines from the gate function and improves separation of concerns.",
      "description": "check_readiness_gates (98 lines) has 6 hard gate checks plus a 12-line advisory block (lines 648-660) that warns about under-specified items (few steps, no scope). The advisory block is not a gate — it never blocks anything. Move it to cmd_backlog_check (the primary caller) and the advisory check in cmd_run. This makes check_readiness_gates a clean pass/fail function.",
      "priority": "could",
      "category": null,
      "steps": [
        "Extract the complexity hints block (step count < 3 advisory, no scope advisory) from check_readiness_gates",
        "Add the advisory checks to cmd_backlog_check after the gate check",
        "Update the advisory check in cmd_run dry-run and sprint loop to call the extracted logic",
        "Verify: backlog check still shows advisory warnings alongside gate failures"
      ],
      "acceptanceCriteria": [
        "check_readiness_gates contains only hard gate checks (title, intent, steps, AC, short steps, deps)",
        "check_readiness_gates does not contain complexity_hints or under-specified logic",
        "backlog check output still includes advisory warnings when relevant",
        "shellcheck passes"
      ],
      "scope": [
        ".aishore/**"
      ],
      "dependsOn": [],
      "status": "todo",
      "passes": false,
      "readyForSprint": true
    }
  ]
}
